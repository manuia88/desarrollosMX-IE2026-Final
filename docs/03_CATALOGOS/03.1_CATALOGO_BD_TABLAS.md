# Catálogo 03.1 — Tablas de Base de Datos

> **Propósito:** Referencia exhaustiva del schema BD del rewrite DesarrollosMX v5 Final. Diseñado limpio desde cero (NO heredado del schema v5.1 con DISC-01..09). Multi-country día 1 (`country_code NOT NULL` en todas las tablas de dominio).
> **Última actualización:** 2026-04-21 (FASE 11 XL — Dominio 17 agregado: 15 índices DMX + 15 tablas nuevas señales derivadas)
> **Mantenedor:** CI (auto-refresh desde `information_schema`) + owner por dominio
> **Scope:** ~112 tablas planificadas (H1 completo) · **62 creadas a la fecha** (FASES 00-07b cerradas) · 50 planeadas (FASES 08-29)

## ⚠️ Estado real vs planeado

> **Este catálogo es aspiracional** (schema completo H1). Para saber qué tablas existen HOY en el remote Supabase, cruzar contra `supabase/migrations/*.sql`. Post FASE 08+ cada nuevo CREATE TABLE migra una tabla planeada de 📋 a ✅.

**Leyenda:**
- ✅ **Creada** — existe en migrations + remote (FASES 00-07b)
- 📋 **Planeada** — documentada aquí, se creará en FASE indicada (08+)
- 🔀 **Alias/rename** — nombre en catálogo difiere de nombre real en migrations (reconciliar)

**Tablas ✅ creadas actualmente (62 total):**

| Dominio | Tablas creadas ✅ |
|---|---|
| 1 Multi-tenant (5) | `profiles`, `desarrolladoras`, `agencies`, `broker_companies`, `addresses` |
| 2 Multi-country meta (6) | `countries`, `currencies`, `locales`, `fx_rates`, `legal_documents_template` 🔀 (cat dice `legal_frameworks`), `tax_rules`, `fiscal_docs` 🔀 (cat dice `fiscal_docs_templates`) |
| 8 IE data (8) | `geo_data_points`, `geo_snapshots`, `macro_series`, `market_prices_secondary`, `market_pulse`, `search_trends`, `zona_snapshots`, `zone_price_index` |
| 10 AI (2) | `ai_memory_store`, `embeddings` 🔀 (cat dice `rag_embeddings`) |
| 13 Sistema & control (8) | `audit_log`, `api_keys`, `api_rate_limits`, `feature_registry`, `plans`, `subscriptions`, `role_features`, `profile_feature_overrides` |
| **14 Buyer PPD + Vibe Tags** (0/8) | **Todas planeadas FASE 20** (micro_questions_catalog, user_micro_question_answers, behavioral_signals, buyer_property_matches, vibe_tags_catalog, property_invitations, buyer_groups + members, reverse_listings) |
| **15 STR Intelligence** ⭐ NUEVO (16) | `str_markets`, `str_listings`, `str_monthly_snapshots`, `str_market_monthly_aggregates`, `str_reviews`, `str_reviews_labels`, `str_photos_metadata`, `str_hosts`, `str_host_migrations`, `str_invisible_hotels`, `str_cost_assumptions`, `str_zone_regulations`, `str_events_calendar`, `str_pricing_overrides`, `str_reports`, `airroi_spend_ledger` |
| **16 Infra & Ops** ⭐ NUEVO (15) | `auth_backup_codes`, `auth_sessions_log`, `role_requests`, `privacy_exports`, `rate_limit_log`, `rate_limit_policies`, `view_dedup`, `ingest_runs`, `ingest_watermarks`, `ingest_dlq`, `ingest_allowed_sources`, `data_lineage`, `api_budgets`, `confidence_thresholds`, `zone_tiers`, `ml_training_snapshots` |

**Tablas 📋 planeadas (50 total):** Proyectos (11: projects, prototipos, unidades, esquemas_pago, precios_unidad, fotos, avance_obra+log, project_brokers/landing_pages/competitors) · CRM asesor (16: contactos, busquedas+sub, visitas, captaciones, propiedades_secundarias, acm_valuaciones, tareas, operaciones+docs+timeline, commission_payments, actividad_timeline, client_folders, interaction_feedback) · Gamification (4) · Marketing (5) · IE core (8: zone_scores, project_scores, user_scores, score_history, score_recalculation_queue, score_subscriptions, market_anomalies, dmx_indices) · IE data extra (2: str_market_data 🔀 redundante con Dominio 15, external_data) · Tracking (8) · AI extra (4) · Contabilidad (8) · Notifs (5) · Dominio 14 PPD (8) · resto de Sistema.

**Reconciliaciones (🔀) pendientes futuras:**
- `legal_frameworks` (cat) → usar `legal_documents_template` (real)
- `fiscal_docs_templates` (cat) → usar `fiscal_docs` (real)
- `rag_embeddings` (cat) → usar `embeddings` (real)
- `str_market_data` (cat 8.5) → **obsoleto**, reemplazado por Dominio 15 completo

---

---

## Resumen

| Dominio | # tablas | `country_code` | RLS | Notas |
|---|---|---|---|---|
| Multi-tenant | 5 | SÍ | SÍ | Profiles, desarrolladoras, broker_companies, agencies, addresses (polimórfica) |
| Multi-country meta | 7 | N/A (son países) | SÍ (read-only) | Países, monedas, locales, FX, legal, fiscal |
| Proyectos | 11 | SÍ | SÍ | Core marketplace |
| CRM asesor | 16 | SÍ | SÍ | Contactos, búsquedas, captaciones |
| Gamification | 4 | SÍ | SÍ | XP + ligas + badges |
| Marketing | 5 | SÍ | SÍ | Landings, QR, templates, folders |
| IE core | 8 | SÍ | SÍ (explícita por plan) | Scores + queue + history |
| IE data | 10 | SÍ | SÍ (read-only para usuarios) | Fuentes externas + snapshots |
| Tracking & growth | 8 | SÍ | SÍ | Events, logs, wishlist |
| AI | 6 | SÍ | SÍ | RAG, prompts, usage |
| Contabilidad | 8 | SÍ | SÍ | CFDI, SAT, bank, payouts |
| Notifs & webhooks | 5 | SÍ | SÍ | 20 tipos + 12 webhooks |
| Sistema & control | 12 | SÍ donde aplica | SÍ | Audit, feature flags, plans |
| Buyer PPD + Vibe Tags + Marketplace social | 8 (incl. buyer_group_members) | SÍ | SÍ | Housekeeping post-07b 2026-04-19 — ADR-021/022 |
| **17 Índices DMX XL + Señales Derivadas** 📋 NUEVO | 16 (1 ALTER dmx_indices + 15 CREATE) | SÍ | SÍ (100%) | FASE 11 XL — Causal Engine, Pulse, Migration Flow, Trend Genome, Genoma colonias pgvector, Constellations, Living Atlas, Climate Twin, LifePath, Widget, Stickers, Ghost Zones, Scorecard Nacional |
| **Total** | **~112** | — | 112/112 (100%) | — |

Cláusula universal:
- Todas las tablas de dominio llevan columnas `created_at timestamptz default now()`, `updated_at timestamptz default now()`, `country_code char(2) references countries(code)`.
- Todas las tablas tienen RLS habilitado (`alter table ... enable row level security`).
- Todo borrado lógico usa `deleted_at timestamptz null` (soft delete).

---

## Dominio 1 — Multi-tenant

### 1.1 `profiles`
| Campo | Valor |
|---|---|
| Propósito | Usuario plataforma. Vinculado 1:1 con `auth.users` de Supabase. Rol determina portal + features. |
| Enum `user_role` | `superadmin`, `admin_desarrolladora`, `asesor`, `broker_manager`, `mb_admin`, `mb_coordinator`, `comprador`, `vendedor_publico` (8 valores). |
| Schema real (FASE 01) | `id uuid pk references auth.users(id) on delete cascade`, `country_code char(2) not null references countries(code)`, `email text not null`, `first_name text not null`, `last_name text not null`, `full_name text generated always as (first_name \|\| ' ' \|\| last_name) stored`, `phone text`, `rol user_role not null default 'comprador'`, `avatar_url text`, `preferred_locale text references locales(code)`, `preferred_timezone text not null default 'America/Mexico_City'`, `preferred_currency char(3) references currencies(code)`, `rfc text`, `tax_id text`, `razon_social text`, `regimen_fiscal text`, `docs_verificacion_urls jsonb`, `is_active bool default true`, `is_approved bool default false`, `desarrolladora_id uuid references desarrolladoras(id) on delete set null`, `agency_id uuid references agencies(id) on delete set null`, `broker_company_id uuid references broker_companies(id) on delete set null`, `slug text unique`, `meta jsonb default '{}'::jsonb`, `psychographic_profile jsonb default '{}'::jsonb` -- Capa 1 answers + formal surveys opcionales [housekeeping post-07b 2026-04-19], `inferred_preferences jsonb default '{}'::jsonb` -- salida behavioral inference engine con confidence per dim [housekeeping post-07b], `buyer_persona text check (buyer_persona in ('first_time','investor','upgrade_family','couple_first','retiree','relocation_regional','relocation_international'))` -- [housekeeping post-07b], `onboarding_wave int default 0` -- 0=no onboarding, 1=Capa 1 done, 2+=Capa 2 partial [housekeeping post-07b], `onboarding_completed bool default false` -- [housekeeping post-07b], `dna_visible bool default true` -- transparency UI toggle Capa 4 [housekeeping post-07b], `life_places jsonb default '[]'::jsonb` -- 5 lugares clave para commute constellation [housekeeping post-07b], `linked_partners uuid[] default '{}'` -- co-match mode [housekeeping post-07b], `receive_invitations bool default true` -- opt-in "La Propiedad me Elige" [housekeeping post-07b], `created_at timestamptz default now()`, `updated_at timestamptz default now()` |
| Adición 2026-04-19 housekeeping post-07b | Columnas nuevas: `psychographic_profile`, `inferred_preferences`, `buyer_persona`, `onboarding_wave`, `onboarding_completed`, `dna_visible`, `life_places`, `linked_partners`, `receive_invitations`. PPD (ADR-021), Vibe Tags (ADR-022), Marketplace social — referenciar Dominio 14 para tablas nuevas relacionadas. |
| country_code | SÍ (NOT NULL) |
| Índices | `(country_code, rol) where is_active` · `lower(email)` · `(slug) where slug is not null` · `(country_code, is_approved, is_active)` · GIN `meta` · GIN trigram `full_name` |
| RLS | FASE 01 stub (SELECT/INSERT self). FASE 02 reemplaza con `profiles_select_self_or_super` (VIEW `public_profiles` sin PII para consumo público) + policy UPDATE self+super con trigger `prevent_role_escalation` bloqueando cambios en `rol`, `is_approved`, `is_active` a no-superadmin. |
| Triggers | `set_updated_at` BEFORE UPDATE · `handle_new_user` AFTER INSERT ON auth.users (SECURITY DEFINER, crea profile con defaults desde raw_user_meta_data) · `prevent_role_escalation` (FASE 02) · `audit_row_change` (FASE 02). |
| Empleadores separados | Campos `desarrolladora_id`, `agency_id`, `broker_company_id` **independientes** (no polimórfico `employer_id`+`employer_type`). Solo uno activo por profile según rol (admin_desarrolladora→desarrolladora_id, broker_manager/mb_*→broker_company_id, asesor→agency_id u opcional broker_company_id). |
| Plan activo | NO columna directa. Se obtiene vía `subscriptions WHERE subject_type='profile' AND subject_id=id AND status IN ('trialing','active')`. |
| MFA | Flag vive en `meta->>'mfa_enabled'` (no columna). Enrollment FASE 02. |
| Fase | FASE 01 (schema base) + FASE 02 (RLS finales + triggers seguridad) |
| Módulo consumidor | Todos (M01-M20) |

### 1.2 `desarrolladoras`
| Campo | Valor |
|---|---|
| Propósito | Empresa desarrolladora (dev portal tenant). |
| Schema real (FASE 01) | `id uuid pk default gen_random_uuid()`, `country_code char(2) not null references countries(code)`, `name text not null`, `legal_name text not null`, `tax_id text not null`, `tax_id_encrypted bytea` (pgsodium FASE 06), `contact_email text`, `contact_phone text`, `website text`, `logo_url text`, `slug text unique`, `is_verified bool default false`, `is_active bool default true`, `verification_docs_urls jsonb`, `holding_parent_id uuid references desarrolladoras(id)` (self-FK para holdings), `meta jsonb default '{}'::jsonb`, `created_at/updated_at timestamptz default now()` |
| country_code | SÍ |
| Índices | `(country_code, is_active)` · `(country_code, tax_id)` · GIN `meta` · GIN trigram `name` |
| RLS | FASE 01 stub (SELECT owner-tenant). FASE 02 amplía con VIEW `public_desarrolladoras` sin `tax_id`/`contact_*`/`verification_docs_urls` (ADR-009 D5). UPDATE solo superadmin + admin_desarrolladora del mismo tenant. |
| Fase | FASE 01 |
| Módulo consumidor | M02, M10, M11, M15 |

### 1.3 `broker_companies`
| Campo | Valor |
|---|---|
| Propósito | Empresa correduría multi-asesor (master broker). Perfiles `broker_manager`/`mb_admin`/`mb_coordinator`/`asesor` pueden apuntar aquí. |
| Schema real (FASE 01) | `id uuid pk default gen_random_uuid()`, `country_code char(2) not null references countries(code)`, `name text not null`, `legal_name text`, `tax_id text`, `contact_email text`, `contact_phone text`, `logo_url text`, `slug text unique`, `is_authorized_broker bool default false`, `is_active bool default true`, `meta jsonb default '{}'::jsonb`, `created_at/updated_at` |
| country_code | SÍ |
| Índices | `(country_code, is_authorized_broker)` · GIN `meta` · GIN trigram `name` |
| Manager | Se infiere vía `profiles WHERE broker_company_id = <id> AND rol = 'broker_manager'`. No columna directa (flexible a múltiples managers). |
| RLS | FASE 01 stub. FASE 02 amplía con VIEW pública sin PII y policies owner + superadmin. |
| Fase | FASE 01 |
| Módulo consumidor | M01, M07, M10 |

### 1.4 `agencies`
| Campo | Valor |
|---|---|
| Propósito | Agencia empleadora de asesores (intermediaria entre asesor y desarrolladora). |
| Schema real (FASE 01) | `id uuid pk default gen_random_uuid()`, `country_code char(2) not null references countries(code)`, `name text not null`, `legal_name text`, `tax_id text`, `contact_email text`, `contact_phone text`, `logo_url text`, `slug text unique`, `is_verified bool default false`, `is_active bool default true`, `meta jsonb default '{}'::jsonb`, `created_at/updated_at` |
| country_code | SÍ |
| Índices | `(country_code, is_active)` · GIN `meta` · GIN trigram `name` |
| RLS | FASE 01 stub. FASE 02 amplía con VIEW pública y policies. |
| Fase | FASE 01 |
| Módulo consumidor | M13, M14 |

### 1.5 `addresses`
| Campo | Valor |
|---|---|
| Propósito | Direcciones polimórficas compartidas entre profiles, desarrolladoras y (en fases posteriores) projects/units. Formato flex vía JSONB `country_sub_divisions`. Geometría PostGIS para búsquedas espaciales. |
| Schema real (FASE 01) | `id uuid pk default gen_random_uuid()`, `country_code char(2) not null references countries(code)`, `owner_type text not null check (owner_type in ('profile','desarrolladora','project','unit','other'))`, `owner_id uuid not null`, `label text`, `line1 text not null`, `line2 text`, `neighborhood text`, `municipality text`, `state text not null`, `postal_code text not null`, `country_sub_divisions jsonb default '{}'::jsonb`, `lat double precision`, `lng double precision`, `geom geometry(Point, 4326) generated always as (st_setsrid(st_makepoint(lng, lat), 4326)) stored`, `is_primary bool default false`, `is_fiscal bool default false`, `created_at/updated_at`, `CHECK public.validate_postal_code(country_code, postal_code)` |
| country_code | SÍ (NOT NULL) |
| Índices | `(country_code, owner_type, owner_id)` · `(country_code, postal_code)` · GIST `geom` |
| Validación | Función `public.validate_postal_code(country_code, postal_code)` IMMUTABLE con regex per país: MX `\d{5}` · CO `\d{6}` · AR CPA o legacy · BR `\d{5}-?\d{3}` · CL `\d{7}` · US `\d{5}(-\d{4})?`. Espejo en Zod client-side. |
| RLS | FASE 01 stub (SELECT owner profile). FASE 02 amplía policies owner polimórfico + superadmin + admin_desarrolladora. Otros `owner_type` (project/unit) en fases 07+. |
| Fase | FASE 01 |

---

## Dominio 2 — Multi-country meta

### 2.1 `countries`
| Campo | Valor |
|---|---|
| Propósito | Catálogo de países soportados (ISO 3166-1 alpha-2). |
| Schema real (FASE 01) | `code char(2) pk`, `name_en text not null`, `name_es text not null`, `name_pt text`, `default_currency char(3) not null references currencies(code) deferrable initially deferred`, `default_locale text not null`, `default_timezone text not null`, `phone_prefix text not null`, `address_format jsonb default '{}'::jsonb`, `fiscal_regime_config jsonb default '{}'::jsonb`, `is_active bool default true`, `created_at timestamptz default now()` |
| country_code | N/A (es el propio catálogo) |
| RLS | SÍ — policy `countries_select_public` para `authenticated, anon` con `USING (is_active = true)`. Write solo superadmin (Fase 02). |
| Fase | FASE 01 |
| Módulo consumidor | Shared `lib/i18n`, `lib/currency`, helpers multi-country |
| Seed (FASE 01) | 6 países: MX (MXN, es-MX, America/Mexico_City, +52), CO (COP, es-CO), AR (ARS, es-AR), BR (BRL, pt-BR), CL (CLP, es-CL), US (USD, en-US) con `address_format` y `fiscal_regime_config` per país (RFC MX, NIT CO, CUIT AR, CNPJ BR, RUT CL, EIN US). |

### 2.2 `currencies`
| Campo | Valor |
|---|---|
| Propósito | ISO 4217. Decimales por moneda (CLP = 0 decimales, resto = 2). |
| Schema real (FASE 01) | `code char(3) pk`, `name_en text not null`, `name_es text not null`, `symbol text not null`, `decimals smallint default 2`, `is_crypto bool default false`, `is_active bool default true` |
| RLS | SÍ — policy `currencies_select_public` para `authenticated, anon` con `USING (is_active = true)`. |
| Fase | FASE 01 |
| Seed (FASE 01) | 7 monedas: MXN, COP, ARS, BRL, CLP (0 decimales), USD, EUR. |

### 2.3 `locales`
| Campo | Valor |
|---|---|
| Propósito | BCP 47 locales — `language[-script]-region`. |
| Schema real (FASE 01) | `code text pk`, `country_code char(2) not null references countries(code)`, `language text not null`, `script text`, `name_native text not null`, `is_rtl bool default false`, `is_active bool default true` |
| RLS | SÍ — policy `locales_select_public` para `authenticated, anon` con `USING (is_active = true)`. |
| Fase | FASE 01 |
| Seed (FASE 01) | 6 locales: es-MX, es-CO, es-AR, pt-BR, es-CL, en-US (todos Latn, no RTL). |

### 2.4 `fx_rates`
| Campo | Valor |
|---|---|
| Propósito | Tasas FX diarias (Open Exchange Rates API + Banxico cache). |
| Schema | `id bigserial pk`, `base_currency char(3)`, `quote_currency char(3)`, `rate numeric`, `fetched_at timestamptz`, `source text`, `unique(base_currency, quote_currency, fetched_at::date)` |
| RLS | SÍ (read-only auth). Insert solo service_role. |
| Fase | FASE 05 |
| Índice | `idx_fx_rates_latest (base_currency, quote_currency, fetched_at desc)` |

### 2.5 `legal_frameworks`
| Campo | Valor |
|---|---|
| Propósito | Descripción legal por país (Mifiel MX, Certicámara CO, AFIP AR, Gov.br BR, SII CL). |
| Schema | `id uuid pk`, `country_code char(2)`, `framework_name text`, `esign_provider text`, `notary_required bool`, `escrow_mechanism text`, `antilavado_threshold_usd numeric`, `ref_law text`, `active_since date` |
| RLS | SÍ (read-only auth) |
| Fase | FASE 18 |

### 2.6 `tax_rules`
| Campo | Valor |
|---|---|
| Propósito | Reglas de IVA/impuestos por país. |
| Schema | `id uuid pk`, `country_code char(2)`, `tax_code text`, `rate numeric`, `applies_to text check (applies_to in ('commission','sale','rent','service'))`, `active_from date`, `active_to date null` |
| Seed MX | IVA commission 16%, ISR retención N/A (asesor independiente). |
| Seed CO | IVA 19% sobre comisión. |
| RLS | SÍ (read-only auth) |
| Fase | FASE 16 |

### 2.7 `fiscal_docs_templates`
| Campo | Valor |
|---|---|
| Propósito | Templates de documentos fiscales por país (CFDI 4.0 MX, Factura DIAN CO, AFIP AR, NFS-e BR, SII CL). |
| Schema | `id uuid pk`, `country_code char(2)`, `doc_type text`, `template_json jsonb`, `required_fields text[]`, `active bool default true` |
| RLS | SÍ (read-only auth) |
| Fase | FASE 16 |

---

## Dominio 3 — Proyectos

### 3.1 `projects`
| Campo | Valor |
|---|---|
| Propósito | Desarrollo inmobiliario (nuevo). Entidad primaria del portal developer. |
| Schema | `id uuid pk`, `desarrolladora_id uuid references desarrolladoras(id)`, `nombre text`, `slug text unique`, `descripcion text`, `direccion text`, `lat numeric`, `lng numeric`, `ciudad text`, `alcaldia text`, `colonia text`, `cp text`, `tipo_propiedad text check (tipo_propiedad in ('vertical','horizontal','mixto','oficinas','comercial'))`, `status text check (status in ('pre_venta','venta','entregado','pausado'))`, `fecha_inicio date`, `fecha_entrega date`, `verificado_at timestamptz null`, `verificado_by uuid null`, `trust_score numeric null`, `quality_score numeric null`, `country_code char(2)`, `currency char(3) default 'MXN'`, `published bool default false`, `vibe_tags jsonb default '[]'::jsonb` -- array vibe_tags ADR-022 (a nivel desarrollo) [housekeeping post-07b], `property_story text` -- narrative del desarrollo (ADR-022) [housekeeping post-07b] |
| Adición 2026-04-19 housekeeping post-07b | Columnas nuevas: `vibe_tags` (ADR-022), `property_story` (ADR-022). Aplica a nivel desarrollo; granularidad por unidad inventario en `unidades`. Ver Dominio 14. |
| country_code | SÍ |
| RLS | SÍ — `projects_select_published_public` (solo `published=true`, sin columnas broker_*), `projects_crud_owner` (desarrolladora dueña), admin override. VIEW `public_projects` sin `broker_commission_pct`, `broker_notes`, `bono_pct`. |
| Fase | FASE 01 |
| Módulo consumidor | M02, M11, M15, M19, M20 |

### 3.2 `prototipos`
| Campo | Valor |
|---|---|
| Propósito | Tipo de unidad dentro de un proyecto (1 rec, 2 rec, penthouse, etc.). |
| Schema | `id uuid pk`, `project_id uuid references projects(id) on delete cascade`, `nombre text`, `m2_totales numeric`, `m2_construccion numeric`, `recamaras int`, `banos numeric`, `estacionamientos int`, `precio_desde numeric`, `precio_hasta numeric`, `country_code char(2)`, `currency char(3)` |
| country_code | SÍ |
| RLS | SÍ (owner + public for published projects) |
| Fase | FASE 01 |

### 3.3 `unidades`
| Campo | Valor |
|---|---|
| Propósito | Unidad individual de inventario. |
| Schema | `id uuid pk`, `project_id uuid references projects(id)`, `prototipo_id uuid references prototipos(id)`, `nivel int`, `numero text`, `m2_totales numeric`, `m2_construccion numeric`, `precio_total numeric`, `currency char(3)`, `status text check (status in ('disponible','apartada','vendida','bloqueada'))`, `broker_exclusive bool default false`, `sold_at timestamptz null`, `country_code char(2)`, `vibe_tags jsonb default '[]'::jsonb` -- array de {code, value?, confidence, data_source, computed_at, edited_by?, edited_reason?} (ADR-022) [housekeeping post-07b], `property_story text` -- AI-generated narrative editable por owner/asesor (ADR-022) [housekeeping post-07b], `property_dating_profile jsonb` -- {name, personality, hobbies, looking_for} M-PROPERTY-DATING-PROFILE [housekeeping post-07b], `match_weights jsonb` -- override opcional dimension weights (ADR-021 PPD) [housekeeping post-07b], `show_address_after_match bool default false` -- M-NDA-FLOW [housekeeping post-07b], `privacy_level text check (privacy_level in ('public','district_only','coords_only','full')) default 'public'` -- M-NDA-FLOW [housekeeping post-07b], `inspection_available bool default false` -- M-INSPECTOR-MARKETPLACE [housekeeping post-07b], `escrow_enabled bool default false` -- M-ESCROW-DIGITAL [housekeeping post-07b] |
| Adición 2026-04-19 housekeeping post-07b | Columnas nuevas: `vibe_tags` (ADR-022), `property_story` (ADR-022), `property_dating_profile`, `match_weights` (ADR-021 PPD), `show_address_after_match`, `privacy_level`, `inspection_available`, `escrow_enabled`. Granularidad por unidad inventario; `vibe_tags`/`property_story` también presentes en `projects` a nivel desarrollo. Ver Dominio 14. |
| country_code | SÍ |
| RLS | SÍ (owner + broker authorized) |
| Fase | FASE 01 |
| Trigger | `log_unit_changes` AFTER INSERT/UPDATE → `unit_change_log`; cascada `unit_sold` y `price_changed` a `score_recalculation_queue`. |

### 3.4 `esquemas_pago`
| Campo | Valor |
|---|---|
| Propósito | Esquemas de financiamiento ofrecidos (contado, mensualidades, apartado+enganche+abonos). |
| Schema | `id uuid pk`, `project_id uuid`, `nombre text`, `apartado_pct numeric`, `enganche_pct numeric`, `mensualidades int`, `descuento_contado_pct numeric null`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ |
| Fase | FASE 15 |

### 3.5 `precios_unidad`
| Campo | Valor |
|---|---|
| Propósito | Historial de precios por unidad (pricing autopilot B03). |
| Schema | `id bigserial pk`, `unidad_id uuid references unidades(id)`, `precio numeric`, `currency char(3)`, `valid_from timestamptz`, `valid_to timestamptz null`, `changed_by uuid`, `reason text null`, `country_code char(2)` |
| RLS | SÍ (owner + admin) |
| Fase | FASE 15 |

### 3.6 `fotos`
| Campo | Valor |
|---|---|
| Propósito | Fotos de proyectos/unidades. Variantes WebP 3 tamaños (400/800/1600). |
| Schema | `id uuid pk`, `project_id uuid null`, `unidad_id uuid null`, `prototipo_id uuid null`, `storage_path_original text`, `storage_path_md text`, `storage_path_lg text`, `variant_set jsonb`, `category text check (category in ('fachada','interior','amenidades','avance_obra','plano','render'))`, `order_index int`, `classified_by text`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ (owner + public for published) |
| Fase | FASE 15 |

### 3.7 `avance_obra`
| Campo | Valor |
|---|---|
| Propósito | Estado porcentual de construcción de un proyecto. |
| Schema | `id uuid pk`, `project_id uuid`, `pct_avance numeric`, `snapshot_at timestamptz default now()`, `notas text`, `uploaded_by uuid`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ (owner read/write; public read si `published`) |
| Fase | FASE 15 |
| Trigger | `trg_avance_obra_log` AFTER INSERT → `avance_obra_log`. |

### 3.8 `avance_obra_log`
| Campo | Valor |
|---|---|
| Propósito | Log append-only (no DELETE RLS) de cambios en avance_obra. |
| Schema | `id bigserial pk`, `avance_obra_id uuid`, `previous_pct numeric`, `new_pct numeric`, `changed_at timestamptz`, `changed_by uuid`, `country_code char(2)` |
| RLS | SÍ (no DELETE) — solo project owner y auditor admin. |
| Fase | FASE 15 |

### 3.9 `project_brokers`
| Campo | Valor |
|---|---|
| Propósito | Autorización explícita de broker/asesor para comercializar proyecto. |
| Schema | `id uuid pk`, `project_id uuid`, `broker_id uuid references profiles(id)`, `exclusive_months int`, `contract_months int`, `commission_pct numeric`, `bono_pct numeric null`, `granted_at timestamptz`, `revoked_at timestamptz null`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ (owner + broker self) — helper `is_authorized_broker(project_id)`. |
| Fase | FASE 15 |

### 3.10 `project_landing_pages`
| Campo | Valor |
|---|---|
| Propósito | Landings personalizadas por asesor/broker para un proyecto. |
| Schema | `id uuid pk`, `project_id uuid`, `created_by uuid references profiles(id)`, `slug text unique`, `template text`, `branding jsonb`, `views int default 0`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ (author + project owner + admin). INSERT valida `is_authorized_broker(project_id)`. |
| Fase | FASE 22 |

### 3.11 `project_competitors`
| Campo | Valor |
|---|---|
| Propósito | Cache de competidores cercanos para B07 Competitive Intel. |
| Schema | `id uuid pk`, `project_id uuid`, `competitor_project_id uuid null`, `competitor_name text`, `lat numeric`, `lng numeric`, `distance_km numeric`, `precio_m2 numeric`, `dimensions jsonb`, `snapshot_at timestamptz`, `country_code char(2)` |
| RLS | SÍ (owner) |
| Fase | FASE 15 |

---

## Dominio 4 — CRM Asesor

### 4.1 `contactos`
| Campo | Valor |
|---|---|
| Propósito | Contacto de cliente (comprador/vendedor/propietario/broker/inversor/otro). |
| Schema | `id uuid pk`, `asesor_id uuid references profiles(id)`, `first_name text`, `last_name text`, `phones jsonb not null default '[]'`, `emails jsonb not null default '[]'`, `tipo text check (tipo in ('comprador','vendedor','propietario','broker','inversor','otro'))`, `temperatura text check (temperatura in ('frio','tibio','caliente','cliente'))`, `tags text[] default '{}'`, `notas text`, `search_vector tsvector`, `origen text`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ — owner (`asesor_id=auth.uid()`) + `get_visible_asesor_ids()` for managers. |
| Fase | FASE 13 |
| Triggers | `check_duplicate_phone` BEFORE INSERT, `update_contactos_search_vector`, `xp_contacto_creado` AFTER INSERT. |
| Índice | `idx_contactos_asesor (country_code, asesor_id)`, `idx_contactos_fts (search_vector) using gin`. |

### 4.2 `busquedas`
| Campo | Valor |
|---|---|
| Propósito | Búsqueda activa de un contacto (comprador o inquilino). |
| Schema | `id uuid pk`, `asesor_id uuid`, `contacto_id uuid references contactos(id)`, `operacion text check (operacion in ('venta','renta'))`, `tipo_propiedad text`, `zonas text[]`, `precio_min numeric`, `precio_max numeric`, `recamaras_min int`, `m2_min numeric`, `fuente text`, `etapa text check (etapa in ('pendiente','buscando','visitando','ofertando','cerrando','ganada','perdida')) default 'pendiente'`, `motivo_perdida text null`, `country_code char(2)`, `currency char(3)` |
| country_code | SÍ |
| RLS | SÍ (owner + equipo) |
| Fase | FASE 13 |
| Trigger | `etapa_advance_audit`, `xp_busqueda_created`. |

### 4.3 `busqueda_contactos`
| Campo | Valor |
|---|---|
| Propósito | N:M — permite compartir una búsqueda entre varios asesores o registrar co-broker. |
| Schema | `busqueda_id uuid`, `contacto_id uuid`, `rol_contacto text`, `pk (busqueda_id, contacto_id)`, `country_code char(2)` |
| RLS | SÍ |
| Fase | FASE 13 |

### 4.4 `busqueda_proyectos`
| Campo | Valor |
|---|---|
| Propósito | Proyectos sugeridos por el matching engine para una búsqueda. |
| Schema | `busqueda_id uuid`, `project_id uuid`, `score numeric`, `rank int`, `asesor_seleccionó bool default false`, `feedback_rating int null`, `country_code char(2)`, `pk (busqueda_id, project_id)` |
| RLS | SÍ |
| Fase | FASE 13 |

### 4.5 `visitas_programadas`
| Campo | Valor |
|---|---|
| Propósito | Agendamiento de visitas a proyecto o prop secundaria. |
| Schema | `id uuid pk`, `asesor_id uuid`, `contacto_id uuid`, `project_id uuid null`, `propiedad_secundaria_id uuid null`, `scheduled_at timestamptz`, `status text check (status in ('programada','confirmada','completada','no_show','cancelada'))`, `feedback text null`, `rating int null`, `country_code char(2)` |
| RLS | SÍ (owner) |
| Fase | FASE 14 |

### 4.6 `captaciones`
| Campo | Valor |
|---|---|
| Propósito | Listado secundario captado por asesor (propiedad en reventa/renta). |
| Schema | `id uuid pk`, `asesor_id uuid`, `contacto_id uuid`, `direccion text`, `tipo_operacion text`, `tipo_propiedad text`, `precio_solicitado numeric`, `currency char(3)`, `etapa text check (etapa in ('pendiente','seguimiento','encuentro','valuacion','documentacion','captado','perdida')) default 'pendiente'`, `urgencia_venta text`, `motivo_perdida text null`, `closed_at timestamptz null`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ (owner + manager) |
| Fase | FASE 14 |
| Regla | INSERT requiere `direccion IS NOT NULL AND tipo_operacion IS NOT NULL AND precio_solicitado > 0` (evitar phantom drafts Pulppo). |

### 4.7 `propiedades_secundarias`
| Campo | Valor |
|---|---|
| Propósito | Propiedad secundaria publicada (captación convertida en listing activo). |
| Schema | `id uuid pk`, `captacion_id uuid references captaciones(id)`, `project_id uuid null`, `slug text unique`, `titulo text`, `descripcion text`, `direccion text`, `lat numeric`, `lng numeric`, `m2_totales numeric`, `precio numeric`, `currency char(3)`, `recamaras int`, `banos numeric`, `published bool default false`, `country_code char(2)` |
| RLS | SÍ (owner + public si `published`) |
| Fase | FASE 14 |

### 4.8 `acm_valuaciones`
| Campo | Valor |
|---|---|
| Propósito | Análisis Comparativo de Mercado generado para una captación o propiedad. |
| Schema | `id uuid pk`, `captacion_id uuid null`, `propiedad_secundaria_id uuid null`, `precio_sugerido numeric`, `precio_salida numeric`, `comparables jsonb`, `generated_at timestamptz`, `generated_by uuid`, `confidence text`, `country_code char(2)`, `currency char(3)` |
| RLS | SÍ (owner + admin) |
| Fase | FASE 14 |

### 4.9 `tareas`
| Campo | Valor |
|---|---|
| Propósito | Tareas del asesor (propiedades/clientes/prospectos/general). |
| Schema | `id uuid pk`, `asesor_id uuid`, `type text check (type in ('property','capture','search','client','lead','general'))`, `title text`, `description text`, `priority text check (priority in ('low','normal','high','urgent')) default 'normal'`, `due_at timestamptz`, `status text check (status in ('pending','in_progress','done','expired','cancelled'))`, `entity_type text null`, `entity_id uuid null`, `recurring jsonb null`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ (owner + manager permissions) |
| Fase | FASE 13 |
| Trigger | `trg_feedback_cascade`, `expire_overdue_tasks` (cron daily). |

### 4.10 `operaciones`
| Campo | Valor |
|---|---|
| Propósito | Operación inmobiliaria (venta/renta). Reemplaza status duplicados viejos. |
| Schema | `id uuid pk`, `code text unique`, `asesor_buyer_id uuid null`, `asesor_seller_id uuid null`, `side text check (side in ('both','buyer','seller')) not null`, `contacto_buyer_id uuid null`, `contacto_seller_id uuid null`, `unidad_id uuid null`, `propiedad_secundaria_id uuid null`, `valor_cierre numeric`, `currency char(3)`, `comision_pct numeric default 4.0`, `iva_pct numeric default 16.0`, `monto_recibido numeric null`, `split_ratio numeric default 0.5`, `status text check (status in ('propuesta','oferta_aceptada','escritura','cerrada','pagando','cancelada'))`, `fecha_cierre_estimada date`, `closed_at timestamptz null`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ — INSERT valida `asesor_buyer_id = auth.uid() OR asesor_seller_id = auth.uid()`. DELETE solo superadmin. |
| Fase | FASE 13 |
| Triggers | `generate_operation_code` BEFORE INSERT, `calculate_commission` BEFORE INSERT/UPDATE, `generate_operation_documents` AFTER INSERT, `webhooks_operaciones_emit`, `xp_operacion_cerrada`. |

### 4.11 `operation_documents`
| Campo | Valor |
|---|---|
| Propósito | Documentos generados o subidos para una operación. |
| Schema | `id uuid pk`, `operacion_id uuid`, `doc_type text`, `storage_path text`, `signed bool default false`, `signed_at timestamptz null`, `signer_email text null`, `provider text check (provider in ('mifiel','docusign','internal'))`, `provider_ref text null`, `country_code char(2)` |
| RLS | SÍ |
| Fase | FASE 18 |

### 4.12 `operation_timeline`
| Campo | Valor |
|---|---|
| Propósito | Timeline append-only de la operación. |
| Schema | `id bigserial pk`, `operacion_id uuid`, `stage text`, `reached_at timestamptz`, `actor_id uuid null`, `notes text`, `country_code char(2)` |
| RLS | SÍ (no DELETE) |
| Fase | FASE 13 |

### 4.13 `commission_payments`
| Campo | Valor |
|---|---|
| Propósito | Programación y ejecución de pagos de comisiones (split plataforma + asesor + inmobiliaria). |
| Schema | `id uuid pk`, `operacion_id uuid`, `recipient_id uuid`, `recipient_type text`, `amount numeric`, `currency char(3)`, `scheduled_for date`, `paid_at timestamptz null`, `status text check (status in ('pending','processing','paid','failed','cancelled'))`, `provider_ref text null`, `country_code char(2)` |
| RLS | SÍ (owner + admin; no DELETE para recipient) |
| Fase | FASE 18 |

### 4.14 `actividad_timeline`
| Campo | Valor |
|---|---|
| Propósito | Timeline cross-entity del asesor (llamadas, notas, interacciones). |
| Schema | `id bigserial pk`, `asesor_id uuid`, `entity_type text`, `entity_id uuid`, `kind text check (kind in ('call','email','whatsapp','note','meeting','visit','task_done'))`, `descripcion text`, `occurred_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner + manager; no DELETE) |
| Fase | FASE 13 |

### 4.15 `client_folders`
| Campo | Valor |
|---|---|
| Propósito | Carpetas digitales de clientes del asesor (dossiers compartibles). |
| Schema | `id uuid pk`, `asesor_id uuid`, `contacto_id uuid`, `nombre text`, `shared_link text unique null`, `pin text null`, `expires_at timestamptz null`, `project_ids uuid[] default '{}'`, `country_code char(2)` |
| RLS | SÍ (owner + shared if pin matches via RPC) |
| Fase | FASE 22 |

### 4.16 `interaction_feedback`
| Campo | Valor |
|---|---|
| Propósito | Feedback post-visita, post-llamada, post-envío. Dispara cascada B04 Product Market Fit. |
| Schema | `id uuid pk`, `asesor_id uuid`, `contacto_id uuid null`, `visita_id uuid null`, `project_id uuid null`, `temperature int check (temperature between 1 and 5)`, `objections text[]`, `notes text`, `country_code char(2)` |
| RLS | SÍ (owner; no DELETE) |
| Fase | FASE 13 |
| Trigger | `trg_feedback_cascade` BEFORE INSERT → enqueue cascade `feedback_registered`. |

---

## Dominio 5 — Gamification

### 5.1 `asesor_gamification`
| Campo | Valor |
|---|---|
| Propósito | Estado XP/level/streak/badges por asesor. Owner-read only. |
| Schema | `user_id uuid pk references profiles(id)`, `xp_total int default 0`, `level int default 1`, `current_streak int default 0`, `longest_streak int default 0`, `badges text[] default '{}'`, `monthly_rank int null`, `league text check (league in ('bronze','silver','gold','diamond'))`, `last_activity_at timestamptz`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ (`user_id=auth.uid()` SELECT only; UPDATE solo por SECURITY DEFINER functions) |
| Fase | FASE 13 |

### 5.2 `badges_catalog`
| Campo | Valor |
|---|---|
| Propósito | Catálogo de badges desbloqueables. |
| Schema | `code text pk`, `name text`, `description text`, `icon text`, `criteria jsonb`, `rarity text check (rarity in ('common','rare','epic','legendary'))`, `country_code char(2) null` |
| RLS | SÍ (read auth) |
| Fase | FASE 13 |

### 5.3 `leaderboards`
| Campo | Valor |
|---|---|
| Propósito | Leaderboards mensuales por zona/país. |
| Schema | `id uuid pk`, `scope text check (scope in ('country','zone','broker_company'))`, `scope_ref text`, `period text`, `leaderboard jsonb`, `generated_at timestamptz`, `country_code char(2)` |
| RLS | SÍ (read auth; write solo cron) |
| Fase | FASE 13 |

### 5.4 `xp_events`
| Campo | Valor |
|---|---|
| Propósito | Registro append-only de eventos XP (para debugging + anti-fraude). |
| Schema | `id bigserial pk`, `user_id uuid`, `event_code text`, `xp_delta int`, `entity_type text null`, `entity_id uuid null`, `occurred_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner read; no DELETE; insert solo SECURITY DEFINER) |
| Fase | FASE 13 |

---

## Dominio 6 — Marketing

### 6.1 `qr_codes`
| Campo | Valor |
|---|---|
| Propósito | QR generados por asesor/broker para landing propia. |
| Schema | `id uuid pk`, `owner_id uuid`, `target_url text`, `short_code text unique`, `scans int default 0`, `campaign text null`, `country_code char(2)` |
| RLS | SÍ (owner + admin; NO public read — usa RPC para redirigir sin leak) |
| Fase | FASE 22 |

### 6.2 `whatsapp_templates`
| Campo | Valor |
|---|---|
| Propósito | Templates WA aprobados por Meta por país. |
| Schema | `id uuid pk`, `owner_id uuid null`, `template_name text`, `language text`, `body text`, `variables text[]`, `meta_status text`, `country_code char(2)` |
| RLS | SÍ |
| Fase | FASE 22 |

### 6.3 `email_templates`
| Campo | Valor |
|---|---|
| Propósito | Templates Resend de email transaccional y marketing. |
| Schema | `id uuid pk`, `owner_id uuid null`, `slug text unique`, `subject text`, `body_html text`, `body_text text`, `country_code char(2)` |
| RLS | SÍ |
| Fase | FASE 22 |

### 6.4 `marketing_portales`
| Campo | Valor |
|---|---|
| Propósito | Configuración de publicación automática a portales externos (Inmuebles24, Vivanuncios, Mercadolibre). |
| Schema | `id uuid pk`, `owner_id uuid`, `portal text`, `credentials_encrypted bytea`, `active bool default true`, `last_sync_at timestamptz`, `country_code char(2)` |
| RLS | SÍ (owner only; credentials encrypted via pgsodium) |
| Fase | FASE 22 |

### 6.5 `campaigns`
| Campo | Valor |
|---|---|
| Propósito | Campañas de marketing con métricas agregadas. |
| Schema | `id uuid pk`, `owner_id uuid`, `name text`, `channel text check (channel in ('whatsapp','email','sms','push','paid_ads'))`, `start_at timestamptz`, `end_at timestamptz null`, `metrics jsonb`, `country_code char(2)` |
| RLS | SÍ (owner + admin) |
| Fase | FASE 22 |

---

## Dominio 7 — IE Core (Scores)

### 7.1 `zone_scores`
| Campo | Valor |
|---|---|
| Propósito | Scores a nivel colonia/AGEB/zona. |
| Schema | `id uuid pk`, `zone_ref text`, `zone_type text check (zone_type in ('colonia','ageb','alcaldia','ciudad','polygon'))`, `score_code text`, `score_level int check (score_level between 0 and 5)`, `value numeric`, `components jsonb`, `confidence text check (confidence in ('high','medium','low','insufficient_data'))`, `tier int check (tier between 1 and 4)`, `valid_from timestamptz`, `valid_until timestamptz null`, `country_code char(2)` |
| country_code | SÍ |
| RLS | SÍ (SELECT público si tier<=2, premium tier>=3). Insert/Update solo service_role. |
| Fase | FASE 08-12 |
| Índice | `idx_zone_scores_lookup (country_code, zone_ref, score_code) where valid_until is null` |

### 7.2 `project_scores`
| Campo | Valor |
|---|---|
| Propósito | Scores a nivel proyecto (B01-B15, E01, G01). |
| Schema | `id uuid pk`, `project_id uuid`, `score_code text`, `score_level int`, `value numeric`, `components jsonb`, `confidence text`, `tier int`, `valid_from timestamptz`, `valid_until timestamptz null`, `country_code char(2)` |
| RLS | SÍ (owner + suscriptores + admin) |
| Fase | FASE 09-12 |

### 7.3 `user_scores`
| Campo | Valor |
|---|---|
| Propósito | Scores personales del comprador (A01, A02, A07, A08, A10, A11, A12, H14). |
| Schema | `id uuid pk`, `user_id uuid`, `score_code text`, `value numeric`, `components jsonb`, `buyer_persona text null`, `valid_from timestamptz`, `country_code char(2)` |
| RLS | SÍ (owner read; service_role write) |
| Fase | FASE 12 |

### 7.4 `score_history`
| Campo | Valor |
|---|---|
| Propósito | Historial inmutable (no DELETE) de todos los scores. |
| Schema | `id bigserial pk`, `score_table text`, `score_id uuid`, `previous_value numeric`, `new_value numeric`, `snapshot_at timestamptz default now()`, `delta_pct numeric`, `country_code char(2)` |
| RLS | SÍ (read auth; no DELETE). Insert por trigger `archive_score_before_update`. |
| Fase | FASE 08 |

### 7.5 `score_recalculation_queue`
| Campo | Valor |
|---|---|
| Propósito | Queue priorizada de recalculo. Deduplicada contra pending/processing. |
| Schema | `id bigserial pk`, `score_type text`, `entity_type text`, `entity_id uuid`, `triggered_by text`, `priority int check (priority between 1 and 10)`, `batch_mode bool default false`, `status text check (status in ('pending','processing','done','failed'))`, `enqueued_at timestamptz default now()`, `processed_at timestamptz null`, `country_code char(2)` |
| RLS | SÍ (read admin; write solo SECURITY DEFINER `enqueue_score_recalc`) |
| Fase | FASE 08 |

### 7.6 `score_subscriptions`
| Campo | Valor |
|---|---|
| Propósito | Suscripciones a alertas de score (watchlist comprador, devs zona). |
| Schema | `id uuid pk`, `user_id uuid`, `score_code text`, `entity_type text`, `entity_id uuid`, `threshold_delta_pct numeric default 5.0`, `channel text[] default '{in_app,email}'`, `active bool default true`, `country_code char(2)` |
| RLS | SÍ (owner). INSERT valida plan tiene feature `score_subscriptions`. |
| Fase | FASE 23 |

### 7.7 `market_anomalies`
| Campo | Valor |
|---|---|
| Propósito | Anomalías detectadas por E04 Anomaly Detector (precios, absorción, crimen, gentrificación). |
| Schema | `id uuid pk`, `anomaly_code text`, `entity_type text`, `entity_id uuid`, `severity text check (severity in ('info','warning','critical'))`, `description text`, `detected_at timestamptz`, `acknowledged_by uuid null`, `country_code char(2)` |
| RLS | SÍ (admin read; service_role write) |
| Fase | FASE 12 |

### 7.8 `dmx_indices`
| Campo | Valor |
|---|---|
| Propósito | Valores mensuales/trimestrales de los 7 índices DMX propietarios. |
| Schema | `id bigserial pk`, `index_code text check (index_code in ('DMX-IPV','DMX-IAB','DMX-IDS','DMX-IRE','DMX-ICO','DMX-MOM','DMX-LIV'))`, `scope text`, `scope_ref text`, `period date`, `value numeric`, `components jsonb`, `rank int null`, `country_code char(2)` |
| RLS | SÍ (read público tier<=2; premium tier>=3). |
| Fase | FASE 11 |

---

## Dominio 8 — IE Data (Fuentes)

### 8.1 `geo_data_points`
| Campo | Valor |
|---|---|
| Propósito | Punto geoespacial genérico (DENUE, FGJ, GTFS, Atlas, SIGED, DGIS, SACMEX). |
| Schema | `id bigserial pk`, `source_code text`, `external_id text`, `lat numeric`, `lng numeric`, `payload jsonb`, `period_date date`, `is_active bool default true`, `country_code char(2)` |
| RLS | SÍ (read auth; write service_role) |
| Fase | FASE 07 |
| Índice | GIST sobre `(lat,lng)`, `idx_geo_source_period (country_code, source_code, period_date)` |

### 8.2 `geo_snapshots`
| Campo | Valor |
|---|---|
| Propósito | Snapshots temporales completos por fuente (para calcular Δ N03 Gentrification Velocity). |
| Schema | `id uuid pk`, `source_code text`, `snapshot_date date`, `total_records int`, `metadata jsonb`, `country_code char(2)` |
| RLS | SÍ (read auth) |
| Fase | FASE 07 |

### 8.3 `macro_series`
| Campo | Valor |
|---|---|
| Propósito | Series macro (Banxico, INEGI, SHF, BBVA, CNBV, Infonavit, FOVISSSTE). |
| Schema | `id bigserial pk`, `source_code text`, `series_code text`, `period date`, `value numeric`, `unit text`, `country_code char(2)` |
| RLS | SÍ (read auth; write service_role) |
| Fase | FASE 07 |

### 8.4 `market_prices_secondary`
| Campo | Valor |
|---|---|
| Propósito | Precios del mercado secundario (Inmuebles24/Mudafy + captaciones propias). |
| Schema | `id bigserial pk`, `source text`, `external_ref text`, `lat numeric`, `lng numeric`, `tipo text`, `m2 numeric`, `precio numeric`, `currency char(3)`, `captured_at timestamptz`, `country_code char(2)` |
| RLS | SÍ (read auth + anon for aggregated) |
| Fase | FASE 07 |

### 8.5 `str_market_data`
| Campo | Valor |
|---|---|
| Propósito | Datos Short Term Rental (AirDNA). |
| Schema | `id bigserial pk`, `zone_ref text`, `adr numeric`, `occupancy_pct numeric`, `revenue_monthly numeric`, `period date`, `country_code char(2)` |
| RLS | SÍ |
| Fase | FASE 07 |

### 8.6 `search_trends`
| Campo | Valor |
|---|---|
| Propósito | Google Trends scraping por colonia + keyword. |
| Schema | `id bigserial pk`, `zone_ref text`, `keyword text`, `trend_value numeric`, `period date`, `country_code char(2)` |
| RLS | SÍ |
| Fase | FASE 07 |

### 8.7 `market_pulse`
| Campo | Valor |
|---|---|
| Propósito | Vista agregada horaria de pulso de mercado (operaciones nuevas, precios, búsquedas). |
| Schema | `id bigserial pk`, `zone_ref text`, `period_hour timestamptz`, `new_ops int`, `avg_price_m2 numeric`, `search_volume int`, `country_code char(2)` |
| RLS | SÍ |
| Fase | FASE 12 |

### 8.8 `zone_price_index`
| Campo | Valor |
|---|---|
| Propósito | Índice de precios por zona (base 100 month 0). |
| Schema | `id bigserial pk`, `zone_ref text`, `period date`, `index_value numeric`, `country_code char(2)` |
| RLS | SÍ |
| Fase | FASE 11 |

### 8.9 `zona_snapshots`
| Campo | Valor |
|---|---|
| Propósito | Snapshots agregados por zona (conteos, promedios) para histórico. |
| Schema | `id bigserial pk`, `zone_ref text`, `snapshot_date date`, `metrics jsonb`, `country_code char(2)` |
| RLS | SÍ |
| Fase | FASE 11 |

### 8.10 `external_data`
| Campo | Valor |
|---|---|
| Propósito | Bucket genérico para datasets externos tercera parte (reports Cushman, CBRE). |
| Schema | `id bigserial pk`, `source text`, `payload jsonb`, `fetched_at timestamptz`, `country_code char(2)` |
| RLS | SÍ (read admin) |
| Fase | FASE 07 |

---

## Dominio 9 — Tracking & Growth

### 9.1 `events`
| Campo | Valor |
|---|---|
| Propósito | Event sourcing ligero (complementario a PostHog) para análisis AARRR. |
| Schema | `id bigserial pk`, `user_id uuid null`, `session_id text`, `event_code text`, `properties jsonb`, `occurred_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner read + admin) |
| Fase | FASE 24 |

### 9.2 `search_logs`
| Campo | Valor |
|---|---|
| Propósito | Búsquedas del marketplace público + comprador. |
| Schema | `id bigserial pk`, `user_id uuid null`, `filters jsonb`, `results_count int`, `clicked_ids uuid[]`, `searched_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner + admin). Rate limit INSERT. |
| Fase | FASE 21 |

### 9.3 `project_views`
| Campo | Valor |
|---|---|
| Propósito | Vistas de ficha de proyecto (deduplicadas por hash session+project+hora). |
| Schema | `id bigserial pk`, `project_id uuid`, `user_id uuid null`, `session_id text`, `viewed_at timestamptz default now()`, `referrer text`, `country_code char(2)`, `unique(session_id, project_id, (viewed_at::date))` |
| RLS | SÍ (owner + admin). INSERT requiere session_id válido (no `qual=true`). |
| Fase | FASE 21 |

### 9.4 `wishlist`
| Campo | Valor |
|---|---|
| Propósito | Wishlist del comprador (individual + compartida con family accounts). |
| Schema | `id uuid pk`, `user_id uuid`, `project_id uuid null`, `unidad_id uuid null`, `propiedad_secundaria_id uuid null`, `shared_with uuid[] default '{}'`, `added_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner + shared_with members) |
| Fase | FASE 20 |

### 9.5 `unit_change_log`
| Campo | Valor |
|---|---|
| Propósito | Log inmutable de cambios en unidades (price_changed, status_changed). |
| Schema | `id bigserial pk`, `unidad_id uuid`, `field text`, `old_value jsonb`, `new_value jsonb`, `changed_at timestamptz default now()`, `changed_by uuid null`, `country_code char(2)` |
| RLS | SÍ (no DELETE; owner + admin read) |
| Fase | FASE 15 |

### 9.6 `inventory_snapshots`
| Campo | Valor |
|---|---|
| Propósito | Snapshot diario de inventario total por proyecto (para B08 Absorption). |
| Schema | `id bigserial pk`, `project_id uuid`, `snapshot_date date`, `total_units int`, `sold_count int`, `available_count int`, `avg_price numeric`, `country_code char(2)` |
| RLS | SÍ (owner + admin) |
| Fase | FASE 15 |

### 9.7 `demand_queries`
| Campo | Valor |
|---|---|
| Propósito | Queries explícitas del usuario al mapa/buscador (diferente a search_logs por contener contexto de zona). |
| Schema | `id bigserial pk`, `user_id uuid null`, `zone_ref text null`, `query jsonb`, `submitted_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner + admin). INSERT rate-limited. |
| Fase | FASE 21 |

### 9.8 `home_alerts`
| Campo | Valor |
|---|---|
| Propósito | Tarjetas de alerta/home prioritarias del asesor (patrón Pulppo). |
| Schema | `id uuid pk`, `user_id uuid`, `kind text check (kind in ('whatsapp_unlinked','visit_unconfirmed','captacion_pending','course_pending','referral_continue','overdue_tasks','momentum_alert'))`, `priority int`, `ref_id uuid null`, `cta_url text`, `dismissed_at timestamptz null`, `country_code char(2)` |
| RLS | SÍ (owner) |
| Fase | FASE 13 |

---

## Dominio 10 — AI

### 10.1 `ai_generated_content`
| Campo | Valor |
|---|---|
| Propósito | Cache/historial de contenido IA (dossiers, briefings, discover weekly, narratives). |
| Schema | `id uuid pk`, `user_id uuid null`, `entity_type text`, `entity_id uuid null`, `content_type text check (content_type in ('dossier','briefing','discover_weekly','narrative','argumentario','objection','wrapped','comparative_report'))`, `content_markdown text`, `content_json jsonb null`, `model_used text`, `tokens_in int`, `tokens_out int`, `generated_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner + admin) |
| Fase | FASE 12 |

### 10.2 `ai_usage_tracking`
| Campo | Valor |
|---|---|
| Propósito | Tracking detallado de tokens + costo por usuario/mes (gating de plan). |
| Schema | `id bigserial pk`, `user_id uuid`, `feature_key text`, `tokens_in int`, `tokens_out int`, `cost_usd numeric`, `occurred_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner read + admin) |
| Fase | FASE 23 |

### 10.3 `ai_prompt_versions`
| Campo | Valor |
|---|---|
| Propósito | Versionado de prompts (A/B testing). |
| Schema | `id uuid pk`, `prompt_key text`, `version text`, `body text`, `active bool`, `notes text`, `country_code char(2) null` |
| RLS | SÍ (admin only) |
| Fase | FASE 12 |

### 10.4 `ai_coaching_log`
| Campo | Valor |
|---|---|
| Propósito | Sugerencias del Copilot al asesor (por sesión, acción accept/reject). |
| Schema | `id bigserial pk`, `user_id uuid`, `suggestion_code text`, `context jsonb`, `accepted bool null`, `occurred_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner) |
| Fase | FASE 03 |

### 10.5 `ai_memory_store`
| Campo | Valor |
|---|---|
| Propósito | Memoria persistente cross-session del Copilot (Anthropic Memory API). |
| Schema | `id uuid pk`, `user_id uuid`, `memory_key text`, `memory_body jsonb`, `updated_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner only) |
| Fase | FASE 03 |

### 10.6 `rag_embeddings`
| Campo | Valor |
|---|---|
| Propósito | Vector embeddings para RAG (docs, proyectos, biblia interna). |
| Schema | `id bigserial pk`, `source_type text`, `source_id text`, `chunk_idx int`, `content text`, `embedding vector(1536)`, `metadata jsonb`, `country_code char(2) null` |
| RLS | SÍ (auth read; service_role write) |
| Fase | FASE 03 |
| Índice | `ivfflat (embedding vector_cosine_ops)` |

---

## Dominio 11 — Contabilidad

### 11.1 `chart_of_accounts`
| Campo | Valor |
|---|---|
| Propósito | Plan de cuentas SAT (catálogo) + personalizado. |
| Schema | `id uuid pk`, `desarrolladora_id uuid null`, `account_code text`, `account_name text`, `parent_id uuid null`, `level int`, `type text check (type in ('asset','liability','equity','revenue','expense'))`, `country_code char(2)` |
| RLS | SÍ (tenant owner + admin) |
| Fase | FASE 16 |

### 11.2 `journal_entries`
| Campo | Valor |
|---|---|
| Propósito | Asientos contables (libro diario). |
| Schema | `id uuid pk`, `desarrolladora_id uuid`, `entry_date date`, `reference text`, `description text`, `lines jsonb not null`, `total_debit numeric`, `total_credit numeric`, `status text check (status in ('draft','posted','voided'))`, `country_code char(2)` |
| RLS | SÍ (owner + contador role) |
| Fase | FASE 16 |

### 11.3 `invoices`
| Campo | Valor |
|---|---|
| Propósito | CFDI MX / Factura DIAN CO / AFIP AR / NFS-e BR / SII CL. |
| Schema | `id uuid pk`, `desarrolladora_id uuid`, `operacion_id uuid null`, `invoice_type text`, `folio text unique`, `uuid_fiscal text unique null`, `rfc_emisor text`, `rfc_receptor text`, `total numeric`, `iva numeric`, `currency char(3)`, `status text check (status in ('draft','stamped','cancelled','paid'))`, `xml text`, `pdf_url text`, `country_code char(2)` |
| RLS | SÍ (owner + admin; no DELETE) |
| Fase | FASE 16 |

### 11.4 `payments`
| Campo | Valor |
|---|---|
| Propósito | Pagos recibidos del comprador (apartado, enganche, mensualidades). |
| Schema | `id uuid pk`, `operacion_id uuid`, `amount numeric`, `currency char(3)`, `method text check (method in ('stripe','mercadopago','transfer','cash','check'))`, `provider_ref text null`, `paid_at timestamptz`, `receipt_url text null`, `country_code char(2)` |
| RLS | SÍ (owner + admin) |
| Fase | FASE 18 |

### 11.5 `payouts`
| Campo | Valor |
|---|---|
| Propósito | Pagos salientes (Stripe Connect split, comisiones asesores). |
| Schema | `id uuid pk`, `desarrolladora_id uuid null`, `recipient_type text`, `recipient_id uuid`, `amount numeric`, `currency char(3)`, `provider text check (provider in ('stripe_connect','mercadopago','manual'))`, `provider_ref text null`, `scheduled_for date`, `paid_at timestamptz null`, `status text check (status in ('scheduled','processing','paid','failed'))`, `country_code char(2)` |
| RLS | SÍ (recipient + admin) |
| Fase | FASE 18 |

### 11.6 `holdbacks`
| Campo | Valor |
|---|---|
| Propósito | Retenciones de comisión hasta que se cumplan ciertas condiciones (escritura firmada, etc.). |
| Schema | `id uuid pk`, `operacion_id uuid`, `amount numeric`, `currency char(3)`, `condition text`, `release_at timestamptz null`, `released bool default false`, `country_code char(2)` |
| RLS | SÍ (owner + admin) |
| Fase | FASE 18 |

### 11.7 `bank_accounts`
| Campo | Valor |
|---|---|
| Propósito | Cuentas bancarias registradas (holding + operadoras). |
| Schema | `id uuid pk`, `desarrolladora_id uuid`, `bank_code text`, `account_number_encrypted bytea`, `clabe_encrypted bytea null`, `currency char(3)`, `country_code char(2)` |
| RLS | SÍ (owner + contador; values encrypted) |
| Fase | FASE 16 |

### 11.8 `bank_transactions`
| Campo | Valor |
|---|---|
| Propósito | Transacciones importadas (OFX/CSV) para reconciliación. |
| Schema | `id bigserial pk`, `bank_account_id uuid`, `tx_date date`, `description text`, `amount numeric`, `currency char(3)`, `matched_payment_id uuid null`, `matched_payout_id uuid null`, `country_code char(2)` |
| RLS | SÍ (owner + contador) |
| Fase | FASE 16 |

---

## Dominio 12 — Notifs & Webhooks

### 12.1 `notificaciones`
| Campo | Valor |
|---|---|
| Propósito | Notificaciones generadas para cada usuario. 20 tipos × 4 canales. |
| Schema | `id uuid pk`, `user_id uuid`, `notif_type text`, `channel text check (channel in ('in_app','email','whatsapp','push'))`, `title text`, `body text`, `payload jsonb`, `read_at timestamptz null`, `sent_at timestamptz`, `delivered bool`, `country_code char(2)` |
| RLS | SÍ (owner) |
| Fase | FASE 22 |

### 12.2 `notificaciones_config`
| Campo | Valor |
|---|---|
| Propósito | Preferencias de opt-in/out por tipo × canal. |
| Schema | `user_id uuid`, `notif_type text`, `channel text`, `enabled bool default true`, `pk (user_id, notif_type, channel)`, `country_code char(2)` |
| RLS | SÍ (owner) |
| Fase | FASE 22 |

### 12.3 `notification_queue`
| Campo | Valor |
|---|---|
| Propósito | Queue de dispatch (processed por cron notif). |
| Schema | `id bigserial pk`, `notif_id uuid`, `status text check (status in ('pending','sending','sent','failed','retry'))`, `attempts int default 0`, `next_retry_at timestamptz null`, `last_error text null`, `country_code char(2)` |
| RLS | SÍ (admin only) |
| Fase | FASE 22 |

### 12.4 `webhooks`
| Campo | Valor |
|---|---|
| Propósito | Webhooks outgoing suscritos por clientes/partners B2B. |
| Schema | `id uuid pk`, `owner_id uuid`, `target_url text`, `event_types text[]`, `secret_hash text`, `active bool default true`, `failure_count int default 0`, `last_success_at timestamptz null`, `country_code char(2)` |
| RLS | SÍ (owner) |
| Fase | FASE 23 |

### 12.5 `webhook_logs`
| Campo | Valor |
|---|---|
| Propósito | Log de ejecución de webhooks (request + response). |
| Schema | `id bigserial pk`, `webhook_id uuid`, `event_type text`, `payload jsonb`, `response_status int`, `response_body text`, `attempt int`, `delivered_at timestamptz`, `country_code char(2)` |
| RLS | SÍ (owner + admin; no DELETE) |
| Fase | FASE 23 |

---

## Dominio 13 — Sistema & Control

### 13.1 `audit_log`
| Campo | Valor |
|---|---|
| Propósito | Log append-only de todas las acciones sensibles. Particionado mensualmente con pg_partman v5. |
| Schema real (FASE 01) | `id bigint generated always as identity`, `country_code char(2) null` (nullable para eventos globales), `actor_id uuid`, `actor_role user_role`, `action text not null`, `table_name text`, `record_id uuid`, `before jsonb`, `after jsonb`, `diff jsonb`, `ip inet`, `user_agent text`, `request_id text`, `meta jsonb default '{}'::jsonb`, `created_at timestamptz default now()`, `primary key (id, created_at)` — PK compuesta exigida por `partition by range (created_at)`. |
| Particionamiento | `public.create_parent('public.audit_log', 'created_at', '1 month')` — pg_partman v5 en schema `public`. |
| Índices | `(actor_id, created_at desc)` · `(table_name, record_id, created_at desc)` · `(country_code, created_at desc)` · GIN `meta`. Se propagan a particiones automáticamente. |
| RLS | FASE 01: `DELETE USING (false)` (append-only, ADR-009 D7). FASE 01 §1.D.1: `SELECT` solo para `is_superadmin()`. FASE 02: trigger `audit_row_change` genérico en tablas sensibles genera rows vía `auth.uid()`. |
| Fase | FASE 01 (schema + partition + helpers) + FASE 02 (trigger genérico) |

### 13.2 `admin_actions`
| Campo | Valor |
|---|---|
| Propósito | Acciones admin (impersonation, bulk ops). Más específico que audit_log. |
| Schema | `id uuid pk`, `admin_id uuid`, `action_type text`, `target_user_id uuid null`, `metadata jsonb`, `occurred_at timestamptz`, `country_code char(2)` |
| RLS | SÍ (admin read; no DELETE) |
| Fase | FASE 19 |

### 13.3 `api_keys`
| Campo | Valor |
|---|---|
| Propósito | API keys para clientes B2B de la API externa v1. |
| Schema | `id uuid pk`, `user_id uuid`, `key_hash text unique`, `key_prefix text`, `plan text check (plan in ('free','starter','pro','enterprise'))`, `requests_today int default 0`, `requests_month int default 0`, `rate_limit_per_min int`, `active bool default true`, `country_code char(2)` |
| RLS | SÍ (owner + admin; key_hash never exposed) |
| Fase | FASE 23 |

### 13.4 `api_request_logs`
| Campo | Valor |
|---|---|
| Propósito | Log de requests a API externa v1. |
| Schema | `id bigserial pk`, `api_key_id uuid`, `endpoint text`, `status_code int`, `response_ms int`, `ip inet`, `occurred_at timestamptz default now()`, `country_code char(2)` |
| RLS | SÍ (owner + admin) |
| Fase | FASE 23 |

### 13.5 `api_rate_limits`
| Campo | Valor |
|---|---|
| Propósito | Rate limiting sliding window. |
| Schema | `api_key_id uuid`, `window_start timestamptz`, `request_count int`, `pk(api_key_id, window_start)`, `country_code char(2)` |
| RLS | SÍ (false a usuarios; service_role only) |
| Fase | FASE 23 |

### 13.6 `plans`
| Campo | Valor |
|---|---|
| Propósito | Catálogo de planes por audience + país. Pricing en menor unidad (centavos). Schema listo en FASE 01; seed real + wire Stripe/MP en FASE 23. |
| Schema real (FASE 01) | `id uuid pk default gen_random_uuid()`, `code text unique not null`, `name text not null`, `audience text not null check (audience in ('asesor','desarrolladora','broker_company','comprador'))`, `country_code char(2) not null references countries(code)`, `monthly_price_minor int not null`, `yearly_price_minor int`, `currency char(3) not null references currencies(code)`, `trial_days smallint default 0`, `is_active bool default true`, `sort_order smallint default 0`, `features_summary jsonb default '[]'::jsonb`, `created_at/updated_at` |
| Índices | `(country_code, audience) where is_active` |
| RLS | FASE 01: `plans_select_public` para `authenticated, anon` (is_active=true). FASE 02: write solo superadmin. |
| Fase | FASE 01 (schema) + FASE 23 (seed + Stripe/MP) |

### 13.7 `subscriptions`
| Campo | Valor |
|---|---|
| Propósito | Suscripción activa de un `subject` polimórfico a un plan. 1 por subject activo. |
| Schema real (FASE 01) | `id uuid pk default gen_random_uuid()`, `country_code char(2) not null references countries(code)`, `subject_type text not null check (subject_type in ('profile','desarrolladora','broker_company'))`, `subject_id uuid not null`, `plan_id uuid not null references plans(id)`, `status text not null check (status in ('trialing','active','past_due','canceled','expired'))`, `billing_cycle text not null check (billing_cycle in ('monthly','yearly'))`, `current_period_start timestamptz not null`, `current_period_end timestamptz not null`, `cancel_at timestamptz`, `canceled_at timestamptz`, `stripe_subscription_id text`, `mercadopago_subscription_id text`, `meta jsonb default '{}'::jsonb`, `created_at/updated_at` |
| Índices | `(country_code, subject_type, subject_id)` · `(status, current_period_end)` · GIN `meta` |
| RLS | FASE 01 stub (SELECT por owner profile). FASE 02 amplía: SELECT por subject owner + superadmin; INSERT/UPDATE solo service_role (via webhook Stripe/MP). |
| Fase | FASE 01 (schema) + FASE 23 (wire payment processors) |

### 13.8 `privacy_consents`
| Campo | Valor |
|---|---|
| Propósito | Consentimientos GDPR/LFPDPPP (MX) por usuario. |
| Schema | `id uuid pk`, `user_id uuid`, `consent_type text`, `granted bool`, `granted_at timestamptz`, `revoked_at timestamptz null`, `country_code char(2)` |
| RLS | SÍ (owner + admin; no DELETE) |
| Fase | FASE 26 |

### 13.9 `feature_registry`
| Campo | Valor |
|---|---|
| Propósito | Catálogo universal de features (120 entries seed FASE 01). Resuelto vía `public.resolve_features(uuid)`. |
| Schema real (FASE 01) | `code text pk` (formato `{category}.{module}.{feature}`), `category text not null` (asesor/dev/admin/comprador/public/shared), `module text not null`, `name_en text not null`, `name_es text not null`, `description_es text`, `is_premium bool default false`, `min_plan text references plans(code)` (NULL hasta FASE 23 wire), `is_beta bool default false`, `is_enabled bool default true`, `h_phase smallint default 1` (1/2/3), `created_at timestamptz default now()` |
| RLS | FASE 01: `feature_registry_select_public` para `authenticated, anon` (is_enabled=true). FASE 02: write solo superadmin. |
| Seed FASE 01 | 120 features: asesor 35, dev 30, admin 15, comprador 15, public 10, shared 15. |
| Fase | FASE 01 |
| Ref | catálogo 03.10 |

### 13.10 `role_features`
| Campo | Valor |
|---|---|
| Propósito | Mapeo rol→features por defecto. Resuelto con `profile_feature_overrides` en `resolve_features()`. |
| Schema real (FASE 01) | `rol user_role not null`, `feature_code text not null references feature_registry(code) on delete cascade`, `is_enabled bool default true`, `primary key (rol, feature_code)` |
| RLS | FASE 01: SELECT público para authenticated (metadata). FASE 02: write solo superadmin. |
| Seed FASE 01 | 432 filas via INSERT…SELECT: superadmin=120, mb_admin=95, broker_manager=50, admin_desarrolladora=45, comprador=39, asesor=35, mb_coordinator=35, vendedor_publico=13. |
| Fase | FASE 01 |

### 13.11 `profile_feature_overrides`
| Campo | Valor |
|---|---|
| Propósito | Override per-profile (beta testers, comp, soporte temporal). Puede habilitar o deshabilitar feature con `expires_at`. |
| Schema real (FASE 01) | `profile_id uuid not null references profiles(id) on delete cascade`, `feature_code text not null references feature_registry(code) on delete cascade`, `is_enabled bool not null`, `reason text`, `granted_by uuid references profiles(id)`, `granted_at timestamptz default now()`, `expires_at timestamptz`, `primary key (profile_id, feature_code)` |
| Índices | `(profile_id, expires_at)` compuesto — sustituye al partial index del plan (que usaba `now()` no-IMMUTABLE). |
| RLS | FASE 01: SELECT por owner (profile_id=auth.uid()). FASE 02: write solo superadmin/mb_admin. |
| Fase | FASE 01 |

### 13.12 `supported_cities`
| Campo | Valor |
|---|---|
| Propósito | Ciudades con datos activos (MX: 16 alcaldías CDMX H1; Monterrey/GDL H2). |
| Schema | `id uuid pk`, `country_code char(2)`, `city_code text`, `name text`, `lat numeric`, `lng numeric`, `active_since date`, `data_completeness_pct numeric` |
| RLS | SÍ (read auth) |
| Fase | FASE 05 |

### 13.13 `metricas_kpi`
| Campo | Valor |
|---|---|
| Propósito | KPIs agregados semanales/mensuales por asesor/zona/plataforma. |
| Schema | `id bigserial pk`, `scope text`, `scope_ref text`, `kpi_code text`, `period_start date`, `period_end date`, `value numeric`, `country_code char(2)` |
| RLS | SÍ (owner + admin) |
| Fase | FASE 13 |

### 13.14 `asesor_status`
| Campo | Valor |
|---|---|
| Propósito | Estado online/offline/visita del asesor (realtime). |
| Schema | `user_id uuid pk`, `status text check (status in ('online','offline','in_visit','do_not_disturb'))`, `last_seen_at timestamptz`, `country_code char(2)` |
| RLS | SÍ (owner write; equipo read) |
| Fase | FASE 13 |

### 13.15 `disc_profiles`
| Campo | Valor |
|---|---|
| Propósito | Perfil DISC del asesor (para matching con buyer persona). |
| Schema | `user_id uuid pk`, `dominant text`, `scores jsonb`, `assessed_at timestamptz`, `country_code char(2)` |
| RLS | SÍ (owner + manager) |
| Fase | FASE 13 |

---

## Dominio 14 — Buyer PPD + Vibe Tags + Marketplace social (housekeeping post-07b 2026-04-19)

> Tablas agregadas en housekeeping post-FASE 07b para soportar Progressive Preference Discovery (ADR-021), Vibe Tags Hybrid (ADR-022) y módulos M-* del BLOQUE 20.L (FASE 20 Portal Comprador). Total 8 tablas nuevas.

### 14.1 `micro_questions_catalog`
| Campo | Valor |
|---|---|
| Propósito | Vocabulario versionado de micro-questions (Capa 2 PPD). NON-ADAPTIVE catalog. |
| Schema | `id uuid pk default gen_random_uuid()`, `code text unique not null`, `question_text jsonb not null` (localized: {es-MX, es-CO, ...}), `options jsonb not null` (localized array), `category text check (category in ('emotional','technical','urban','financial','spatial','investment'))`, `dimension_weight numeric(3,2) not null`, `trigger_conditions jsonb default '{}'::jsonb` (patterns + thresholds + dwell cutoffs), `priority int default 100`, `cooldown_days int default 7`, `is_active bool default true`, `country_code char(2)`, `created_at/updated_at` |
| country_code | SÍ (preguntas pueden ser localizadas por país) |
| RLS | SELECT auth (read-only catalog). Write solo superadmin. |
| Índices | `(country_code, is_active, priority)` · GIN `trigger_conditions` |
| Fase | FASE 20 (housekeeping post-07b) |

### 14.2 `user_micro_question_answers`
| Campo | Valor |
|---|---|
| Propósito | Respuestas Capa 2 PPD per user. Append-only para audit + train. |
| Schema | `id uuid pk default gen_random_uuid()`, `user_id uuid not null references profiles(id) on delete cascade`, `question_code text not null references micro_questions_catalog(code)`, `answer jsonb not null`, `context jsonb` (property_id + route + session_signals snapshot at answer time), `country_code char(2)`, `answered_at timestamptz default now()` |
| Constraint | `unique (user_id, question_code, answered_at)` (permite re-answer en distinto timestamp) |
| RLS | SELECT/INSERT owner. Mb_admin/superadmin read-only. NO DELETE. |
| Índices | `(user_id, question_code, answered_at desc)` · `(country_code, question_code)` |
| Trigger | `audit_row_change` (ADR-018 R7) |
| Fase | FASE 20 (housekeeping post-07b) |

### 14.3 `behavioral_signals`
| Campo | Valor |
|---|---|
| Propósito | Eventos behavioral PPD Capa 3 (dwell, photo_zoom, map_expand, save, skip, contact, price_skip, story_read). Particionada mensual via pg_partman. |
| Schema | `id uuid default gen_random_uuid()`, `user_id uuid not null references profiles(id) on delete cascade`, `signal_type text not null check (signal_type in ('dwell','photo_zoom','map_expand','save','skip','contact','price_skip','story_read','moodboard_like'))`, `property_id uuid null`, `signal_data jsonb default '{}'::jsonb`, `country_code char(2)`, `created_at timestamptz not null default now()`, `primary key (id, created_at)` |
| Particionamiento | `partition by range (created_at)` — mensual via pg_partman, retention 12 meses (LFPDPPP). |
| Índices | BRIN `(user_id, created_at)` · BRIN `(signal_type, created_at)` |
| RLS | INSERT propio (user_id = auth.uid()). SELECT owner + cron service role. NO surface en exports user-facing. |
| Privacy | NO loguear en Sentry. NO incluir en LFPDPPP exports user-facing. |
| Fase | FASE 20 (housekeeping post-07b) — pg_partman setup en FASE 24 SRE |

### 14.4 `buyer_property_matches`
| Campo | Valor |
|---|---|
| Propósito | Output match_score 6D per (user, property). Cache + audit trail. |
| Schema | `id uuid pk default gen_random_uuid()`, `user_id uuid not null references profiles(id) on delete cascade`, `property_id uuid not null` (FK polimórfico — references unidades(id) o projects(id) via property_type), `property_type text check (property_type in ('unidad','project'))`, `match_score_total numeric(5,2) not null check (match_score_total between 0 and 100)`, `dimensions jsonb not null` (6D breakdown: {emotional: {score, weight, reasons[], confidence}, ...}), `inference_contribution jsonb` ({% from behavioral, % from formal, % from defaults}), `confidence_level text check (confidence_level in ('high','medium','low','insufficient'))`, `country_code char(2)`, `computed_at timestamptz default now()` |
| Constraint | `unique (user_id, property_id, property_type)` |
| Índices | `(user_id, match_score_total desc)` · `(property_id, property_type)` |
| RLS | SELECT owner + asesor del lead. Mb_admin/superadmin admin. NO write directo (cron + tRPC procedure recompute). |
| Fase | FASE 20 (housekeeping post-07b) |

### 14.5 `vibe_tags_catalog`
| Campo | Valor |
|---|---|
| Propósito | Vocabulario versionado vibe tags (ADR-022). 50-60 tags seed inicial en 8 categorías. |
| Schema | `id uuid pk default gen_random_uuid()`, `code text unique not null`, `label jsonb not null` (localized), `category text not null check (category in ('lifestyle','inversion','ambiental','social','conectividad','seguridad','amenidades','demografia'))`, `validation_rule jsonb not null` ({type, thresholds, data_source_required[]}), `data_source text not null`, `confidence_threshold numeric(3,2) default 0.5`, `version int default 1`, `is_active bool default true`, `country_code char(2)`, `created_at/updated_at` |
| RLS | SELECT auth (read-only). Write solo superadmin. |
| Índices | `(country_code, category, is_active)` · GIN `validation_rule` |
| Seed | FASE 20 mini-fase dedicada — 50-60 tags iniciales (8 categorías ADR-022) |
| Fase | FASE 20 (housekeeping post-07b) — seed en mini-fase dedicada o pre-FASE 20 |

### 14.6 `property_invitations`
| Campo | Valor |
|---|---|
| Propósito | M-INVERSE-SELECTION ("La Propiedad me Elige"). Owner invita top matches. |
| Schema | `id uuid pk default gen_random_uuid()`, `property_id uuid not null` (polimórfico unidad o project), `property_type text check (property_type in ('unidad','project'))`, `buyer_id uuid not null references profiles(id) on delete cascade`, `invited_by uuid not null references profiles(id)` (owner o asesor), `message text`, `status text check (status in ('sent','accepted','declined','expired')) default 'sent'`, `country_code char(2)`, `sent_at timestamptz default now()`, `responded_at timestamptz`, `expires_at timestamptz default (now() + interval '14 days')` |
| Constraint | `unique (property_id, property_type, buyer_id)` (1 invitación per propiedad por buyer) |
| RLS | SELECT involucrados (invited_by, buyer_id) + admin. INSERT invited_by con rate limit (max 5/day per property). |
| Trigger | `audit_row_change` (ADR-018 R7) + `notify_buyer_on_invitation` |
| Fase | FASE 20 (housekeeping post-07b) |

### 14.7 `buyer_groups` + `buyer_group_members`
| Campo | Valor |
|---|---|
| Propósito | M-GROUP-BUYING. Compradores agrupados por edificio/desarrollo para negociación grupal. |
| Schema `buyer_groups` | `id uuid pk default gen_random_uuid()`, `target_development_id uuid references projects(id)`, `target_building_id uuid null`, `threshold_members int default 5`, `status text check (status in ('forming','active','negotiating','closed')) default 'forming'`, `country_code char(2)`, `created_at/updated_at` |
| Schema `buyer_group_members` | `group_id uuid not null references buyer_groups(id) on delete cascade`, `buyer_id uuid not null references profiles(id) on delete cascade`, `joined_at timestamptz default now()`, `left_at timestamptz null`, `primary key (group_id, buyer_id)` |
| RLS | `buyer_groups`: SELECT members + admin. `buyer_group_members`: SELECT same group + owner. |
| Trigger | `update_group_status_on_member_change` (forming→active al alcanzar threshold) |
| Fase | FASE 20 (housekeeping post-07b) |

### 14.8 `reverse_listings`
| Campo | Valor |
|---|---|
| Propósito | M-REVERSE-AUCTION. Compradores publican demanda; propietarios compiten ofertando properties. |
| Schema | `id uuid pk default gen_random_uuid()`, `buyer_id uuid not null references profiles(id) on delete cascade`, `demand_spec jsonb not null` ({zonas[], presupuesto, specs requeridas, timeline, must_have, nice_to_have}), `status text check (status in ('active','fulfilled','expired')) default 'active'`, `country_code char(2)`, `created_at timestamptz default now()`, `expires_at timestamptz default (now() + interval '60 days')`, `fulfilled_with_property_id uuid null` |
| RLS | SELECT public si `status='active'` (anonimizado buyer); INSERT/UPDATE owner buyer + admin. |
| Índices | GIN `demand_spec` · `(country_code, status, expires_at)` |
| Fase | FASE 20 (housekeeping post-07b) |

---

## Dominio 15 — STR Intelligence (FASE 07b — creada completa)

> **Estado:** ✅ 16 tablas creadas en migrations `20260419080000_str_schema_base.sql` → `20260419093000_str_public_policies_comments.sql`. Backend operativo. UI real difierida a FASE 20 (stubs 4-señales ADR-018 ya cableados).

| # | Tabla | Propósito | RLS |
|---|---|---|---|
| 15.1 | `str_markets` | Catálogo markets AirROI (ciudad/colonia) con external_id | SELECT public (intentional_public documented) |
| 15.2 | `str_listings` | Listings individuales Airbnb scraping via AirROI | Admin + service_role |
| 15.3 | `str_monthly_snapshots` | Snapshots mensuales ADR + occupancy + revenue per listing | Admin + service_role |
| 15.4 | `str_market_monthly_aggregates` | Agregados mensuales per market (medianas) | SELECT public (intentional) |
| 15.5 | `str_reviews` | Reviews per listing (sentiment input) | Admin + service_role |
| 15.6 | `str_reviews_labels` | Labels NLP (sentiment + topics) output Anthropic | Admin |
| 15.7 | `str_photos_metadata` | Metadata fotos (Claude Vision classification: quality_score, staging) | Admin + service_role |
| 15.8 | `str_hosts` | Hosts AirROI con tier + portfolio stats | Admin |
| 15.9 | `str_host_migrations` | Host migration tracking (super-host movements between markets) | Admin |
| 15.10 | `str_invisible_hotels` | Buildings detectados >20% saturación STR (cluster) | Admin |
| 15.11 | `str_cost_assumptions` | Reference data costos típicos STR por país (cleaning, mgmt fee, etc.) | SELECT public (intentional) |
| 15.12 | `str_zone_regulations` | Info regulatoria STR por zona (CDMX reglamento alojamiento) | SELECT public (intentional) |
| 15.13 | `str_events_calendar` | Eventos MX 2026 impact STR pricing (F1, Semana Santa, NFL MX) | SELECT authenticated (intentional_public seed) |
| 15.14 | `str_pricing_overrides` | Dynamic pricing calendar overrides per listing | Owner/host |
| 15.15 | `str_reports` | Reports B2B generados (4 tiers: individual / alcaldía / CDMX / API) | Owner + admin |
| 15.16 | `airroi_spend_ledger` | Cost tracker granular per API call AirROI (provenance + x-amzn-requestid) | Admin |

**Upgrades implementados:** U1 MCP-First (ADR-020), U2 colonia-level markets (7 sub-colonias CDMX seed), U3 export_market MCP-only validation, U4 MCP server backend, U5 cost tracker granular, U6 provenance metadata.

**Referencias:** [ADR-019 STR Module Complete](../01_DECISIONES_ARQUITECTONICAS/ADR-019_STR_MODULE_COMPLETE.md) · [FASE 07b](../02_PLAN_MAESTRO/FASE_07b_STR_INTELLIGENCE_COMPLETE.md) · 17 bloques (7b.A → 7b.P + U1-U6)

---

## Dominio 16 — Infraestructura & Ops (FASES 02, 06, 07 — creada)

> **Estado:** ✅ 15 tablas creadas a lo largo de FASE 02 (Auth), FASE 06 (Seguridad) y FASE 07 (Ingesta). Backend de plataforma que no son "dominio de negocio" sino infra/ops/auditoría.

| # | Tabla | FASE | Propósito | Particionada |
|---|---|---|---|---|
| 16.1 | `auth_backup_codes` | 02 | Códigos MFA backup (10 por user, encrypted) | — |
| 16.2 | `auth_sessions_log` | 02 | Log sesiones auth (sign-in/sign-out) + edge function on_auth_event | — |
| 16.3 | `role_requests` | 02 | Workflow aprobación role escalation (approve/reject by admin) | — |
| 16.4 | `privacy_exports` | 06 | ARCO data-export requests + state machine | — |
| 16.5 | `rate_limit_log` | 02 | Log intentos rate limit (service_role only) | pg_partman diaria |
| 16.6 | `rate_limit_policies` | 06 | Policies rate limit per endpoint/user/ip | — |
| 16.7 | `view_dedup` | 06 | Anti-abuse: dedup views (register_view + view_dedup stored hash) | — |
| 16.8 | `ingest_runs` | 07 | Ingest orchestrator runs + correlation_id (OTel) + cost | — |
| 16.9 | `ingest_watermarks` | 07 | Watermarks per source (last_fetched_at + ETag) para auto-fetch | — |
| 16.10 | `ingest_dlq` | 07 | Dead Letter Queue ingest (jobs failed) + retry counter | — |
| 16.11 | `ingest_allowed_sources` | 07 | Allowlist sources autorizados (Habi explicit DENY vía constraint) | — |
| 16.12 | `data_lineage` | 07 | Provenance rows ingested (source + x-amzn-requestid + cost) | — |
| 16.13 | `api_budgets` | 07 | Budget tracker granular per source (alert 50/80/100%) | — |
| 16.14 | `confidence_thresholds` | 07 | Thresholds confidence per source/signal per country | — |
| 16.15 | `zone_tiers` | 07 | Tiers per zona (premium/standard/basic) por pg_cron recompute nightly | — |
| 16.16 | `ml_training_snapshots` | 07 | Snapshots data para futuros ML training H3 (corpus) | — |

**Observación:** Estas 16 tablas (15 visibles + ml_training_snapshots) NO son "dominios de negocio" sino infra/ops crítica. Son el backbone que habilita los dominios 1-15. RLS siempre ON; mayormente service_role + admin, con excepciones documentadas.

---

## Dominio 17 — Índices DMX XL + Señales Derivadas (FASE 11 XL — planeada)

> **Estado:** 📋 Planeada FASE 11 XL. Expansión del sistema de índices (7 → 15), causal engine, pulse/migration/trend moonshots, genoma de colonias, zone constellations, living atlas, climate twin, lifepath, widget + stickers + alert radar. Todas las tablas nacen con `ENABLE ROW LEVEL SECURITY`.
>
> **Scope agregación:** `scope_type` pasa de `('colonia','alcaldia','ciudad')` a `('colonia','alcaldia','ciudad','estado')` en todas las tablas de agregados (cuatro niveles).
>
> **Políticas RLS recurrentes:**
> - SELECT público: permitido solo para filas de `period_date` cerrado (publicadas) + scope ≤ municipio, excepto tablas explícitamente de usuario (`lifepath_user_profiles`, `zone_alert_subscriptions`) que usan owner policy.
> - INSERT/UPDATE/DELETE: `service_role` (calculators + crons) + superadmin con audit obligatorio.
> - Contenido editorial (`colonia_wiki_entries`) requiere flag `reviewed=true` para SELECT público; writes `service_role` + editor (FASE 15+).

### 17.1 `dmx_indices` (AMPLIADA)

| Campo | Valor |
|---|---|
| Propósito | Valores mensuales/trimestrales/anuales de los **15 índices DMX** propietarios (7 originales + 8 FASE 11 XL). |
| Schema FASE 11 XL | `id bigserial pk`, `index_code text not null check (index_code in ('DMX-IPV','DMX-IAB','DMX-IDS','DMX-IRE','DMX-ICO','DMX-MOM','DMX-LIV','DMX-FAM','DMX-YNG','DMX-GRN','DMX-STR','DMX-INV','DMX-DEV','DMX-GNT','DMX-STA'))`, `scope_type text not null check (scope_type in ('colonia','alcaldia','ciudad','estado'))`, `scope_id text not null`, `period date not null`, `frequency text not null check (frequency in ('monthly','quarterly','yearly'))`, `value numeric not null`, `components jsonb not null`, `rank int null`, `rank_total int null`, `confidence text check (confidence in ('high','medium','low','insufficient_data'))`, `methodology_version text not null default 'v1'`, `country_code char(2) not null references countries(code)`, `created_at timestamptz default now()`, `unique(index_code, scope_type, scope_id, period, methodology_version)` |
| Migración (FASE 11 XL) | `ALTER TYPE` del CHECK + agregar columnas `scope_type='estado'`, `frequency`, `methodology_version`, `rank_total`, `confidence`. Backfill v1 para registros previos. |
| country_code | SÍ (NOT NULL) |
| Índices | `(country_code, scope_type, scope_id, period DESC)` · `(index_code, period DESC, scope_type)` · `(index_code, country_code, period DESC) where rank is not null` · GIN `components` |
| Particionamiento | pg_partman por `period` (monthly) cuando >10M filas (H2). |
| RLS | SELECT público filas `period` cerrado + `scope_type in ('colonia','alcaldia','ciudad')`; `estado` gated superadmin hasta publish formal. INSERT/UPDATE service_role + superadmin. |
| Fase | FASE 11 XL (amplía tabla creada parcialmente en FASE 11 clásica). |
| Módulo consumidor | `/indices/[code]` · Widget embebible · Stickers · Scorecard Nacional |

### 17.2 `zone_migration_flows`

| Campo | Valor |
|---|---|
| Propósito | Flujos de migración origen→destino entre scopes geográficos (INEGI ENADID + RPP traslados de dominio + INE cambios de credencial + LinkedIn moves). Insumo `mig_flow_v1` + DMX-GNT/DMX-YNG/DMX-STA. |
| Schema | `id bigserial pk`, `origin_scope_type text check in ('colonia','alcaldia','ciudad','estado','country')`, `origin_scope_id text not null`, `dest_scope_type text check in ('colonia','alcaldia','ciudad','estado')`, `dest_scope_id text not null`, `period_date date not null`, `volume int not null`, `direction text check in ('in','out','net')`, `demographic_segment jsonb` (age_bucket, income_bucket), `confidence text check in ('high','medium','low')`, `source_mix jsonb not null` (e.g. `{"enadid":0.5,"rpp":0.3,"ine":0.15,"linkedin":0.05}`), `country_code char(2) not null references countries(code)`, `created_at timestamptz default now()` |
| Índices | `(country_code, dest_scope_type, dest_scope_id, period_date DESC)` · `(origin_scope_type, origin_scope_id, period_date DESC)` · GIN `demographic_segment` · GIN `source_mix` |
| RLS | SELECT público agregados `alcaldia`+`ciudad`; `colonia` tier 2+ (≥10 proyectos). service_role write. |
| Fase | FASE 11 XL (bloque 11.G Migration Flow) |

### 17.3 `zone_pulse_scores`

| Campo | Valor |
|---|---|
| Propósito | Pulse Score diario/semanal por colonia (signal-fusion: DENUE births/deaths + permisos + Popular Times + 911 + events). Señal temprana de momentum. |
| Schema | `id bigserial pk`, `colonia_id uuid not null references zonas(id)` 🔀 (o `zone_ref text` si zonas no existe al crear), `period_date date not null`, `business_births int default 0`, `business_deaths int default 0`, `foot_traffic_day numeric`, `foot_traffic_night numeric`, `calls_911_count int default 0`, `events_count int default 0`, `permits_count int default 0`, `pulse_score numeric not null check (pulse_score between 0 and 100)`, `delta_7d numeric`, `delta_30d numeric`, `confidence text check in ('high','medium','low')`, `components jsonb not null`, `country_code char(2) not null references countries(code)`, `created_at timestamptz default now()`, `unique(colonia_id, period_date)` |
| Índices | `(country_code, colonia_id, period_date DESC)` · `(country_code, period_date DESC, pulse_score DESC)` · GIN `components` |
| Particionamiento | pg_partman monthly por `period_date` (alto volumen: 16K colonias × 365 días ≈ 5.8M filas/año). |
| RLS | SELECT público período cerrado (>7d) tier 1; último 7d tier 2+. INSERT service_role. |
| Fase | FASE 11 XL (bloque 11.F Pulse Score) |

### 17.4 `zone_alpha_alerts`

| Campo | Valor |
|---|---|
| Propósito | Alertas zona "pre-mainstream" (Trend Genome detection). Detectadas vía Pulse spike + Influencer Heat + search anomalies cuando una zona emerge antes que adoption masivo. |
| Schema | `id uuid pk default gen_random_uuid()`, `zone_id uuid not null`, `alpha_score numeric not null check (alpha_score between 0 and 100)`, `time_to_mainstream_months int`, `signals jsonb not null` (pulse, heat, search, permits, influencer), `detected_at timestamptz default now()`, `subscribers int default 0`, `status text check in ('detected','notified','mainstream','expired') default 'detected'`, `country_code char(2) not null references countries(code)` |
| Índices | `(country_code, status, alpha_score DESC)` · `(zone_id, detected_at DESC)` · GIN `signals` |
| RLS | SELECT gated Pro+ (premium alert); teaser público solo count. INSERT service_role. |
| Fase | FASE 11 XL (bloque 11.T Alert Radar) |

### 17.5 `influencer_heat_zones`

| Campo | Valor |
|---|---|
| Propósito | Heat map "cool factor" por zona — conteos ponderados de chefs, galerías, creators, specialty cafés (scraping Instagram + DENUE classificado propietario). Insumo Trend Genome. |
| Schema | `id bigserial pk`, `zone_id uuid not null`, `period_date date not null`, `chef_count int default 0`, `gallery_count int default 0`, `creator_count int default 0`, `specialty_cafe_count int default 0`, `designer_count int default 0`, `heat_score numeric not null check (heat_score between 0 and 100)`, `delta_12m numeric`, `sources jsonb not null` (instagram_handles[], denue_ids[]), `country_code char(2) not null references countries(code)`, `created_at timestamptz default now()`, `unique(zone_id, period_date)` |
| Índices | `(country_code, period_date DESC, heat_score DESC)` · `(zone_id, period_date DESC)` · GIN `sources` |
| RLS | SELECT público agregado heat_score; `sources` (handles) gated admin (PII scraping). service_role write. |
| Fase | FASE 11 XL (bloque 11.I Influencer Heat) |

### 17.6 `colonia_dna_vectors`

| Campo | Valor |
|---|---|
| Propósito | Vector embedding 64-dim por colonia (pgvector). Features: scores + índices + demografía + densidad + heat + pulse. Permite "zonas similares al ADN de X" con cosine similarity. |
| Schema | `colonia_id uuid pk references zonas(id)`, `vector vector(64) not null`, `computed_at timestamptz not null default now()`, `components jsonb not null` (raw features + weights), `model_version text not null default 'v1'`, `country_code char(2) not null references countries(code)` |
| Índices | **HNSW/IVFFlat pgvector** sobre `vector` (metric=cosine) — `create index on colonia_dna_vectors using hnsw (vector vector_cosine_ops)`. `(country_code)` btree para filtrado previo. |
| RLS | SELECT público vector + similarity search; `components` gated admin. service_role write. |
| Fase | FASE 11 XL (bloque 11.J Genoma Colonias) |
| Extensión | `create extension if not exists vector;` (ya habilitada H1). |

### 17.7 `zone_constellations_edges`

| Campo | Valor |
|---|---|
| Propósito | Grafo dirigido de relaciones inter-colonias: `commute`, `referral` (búsquedas saltan de A→B), `migration` (household moves). Visualización "constelación" + scoring de cohesión. |
| Schema | `id bigserial pk`, `source_colonia_id uuid not null`, `target_colonia_id uuid not null`, `edge_weight numeric not null check (edge_weight between 0 and 1)`, `edge_types jsonb not null` (e.g. `{"commute":0.4,"referral":0.3,"migration":0.3}`), `period_date date not null`, `country_code char(2) not null references countries(code)`, `created_at timestamptz default now()`, `unique(source_colonia_id, target_colonia_id, period_date)`, `check (source_colonia_id <> target_colonia_id)` |
| Índices | `(country_code, source_colonia_id, edge_weight DESC)` · `(target_colonia_id, period_date DESC)` · GIN `edge_types` |
| RLS | SELECT público período cerrado. service_role write. |
| Fase | FASE 11 XL (bloque 11.K Zone Constellations) |

### 17.8 `colonia_wiki_entries`

| Campo | Valor |
|---|---|
| Propósito | Living Atlas — wiki versionada por colonia. Secciones: history, personalities, music, film, photos. Render markdown + media. |
| Schema | `id uuid pk default gen_random_uuid()`, `colonia_id uuid not null`, `version int not null default 1`, `content_md text not null`, `sections jsonb not null default '{}'::jsonb` (history, personalities[], music[], film[], photos[{url, caption, license}]), `edited_by uuid references profiles(id)`, `edited_at timestamptz default now()`, `reviewed bool default false`, `reviewed_by uuid references profiles(id)`, `reviewed_at timestamptz`, `country_code char(2) not null references countries(code)`, `unique(colonia_id, version)` |
| Índices | `(country_code, colonia_id, version DESC)` · `(reviewed, country_code)` · GIN `sections` · GIN trigram `content_md` |
| RLS | SELECT público `reviewed=true` solamente; drafts solo editor + superadmin. INSERT editor (feature:wiki_editor) + superadmin. |
| Fase | FASE 11 XL (bloque 11.L Living Atlas) |

### 17.9 `zone_alert_subscriptions`

| Campo | Valor |
|---|---|
| Propósito | Suscripciones usuario a Alert Radar WhatsApp/Email/Push per zona con threshold % de cambio de score/índice. |
| Schema | `id uuid pk default gen_random_uuid()`, `user_id uuid not null references profiles(id) on delete cascade`, `zone_id uuid not null`, `threshold_pct numeric not null check (threshold_pct > 0 and threshold_pct <= 100)`, `indices text[] not null default '{DMX-IPV,DMX-MOM}'`, `channel text not null check (channel in ('whatsapp','email','push'))`, `channel_ref text` (phone/email/push_token), `active bool default true`, `last_triggered_at timestamptz`, `trigger_count int default 0`, `country_code char(2) not null references countries(code)`, `created_at timestamptz default now()`, `updated_at timestamptz default now()`, `unique(user_id, zone_id, channel)` |
| Índices | `(user_id, active) where active` · `(zone_id, active) where active` · `(channel, active)` |
| RLS | SELECT/UPDATE/DELETE self (owner) + admin. INSERT self con rate limit. |
| Fase | FASE 11 XL (bloque 11.T Alert Radar) |

### 17.10 `causal_explanations`

| Campo | Valor |
|---|---|
| Propósito | Narrativas causales generadas por LLM (Claude Haiku/Sonnet) explicando POR QUÉ un score/índice cambió. Siempre con citations estructuradas (lineage + fuentes). TTL para invalidación al cambiar upstream. |
| Schema | `id uuid pk default gen_random_uuid()`, `score_id text not null`, `scope_type text not null check in ('colonia','alcaldia','ciudad','estado')`, `scope_id text not null`, `period_date date not null`, `explanation_md text not null`, `citations jsonb not null` (array of `{source, url, claim, lineage_node}`), `model text not null`, `prompt_version text not null`, `tokens_in int`, `tokens_out int`, `cost_usd numeric`, `generated_at timestamptz default now()`, `ttl_days int not null default 30`, `expires_at timestamptz generated always as (generated_at + (ttl_days \|\| ' days')::interval) stored`, `country_code char(2) not null references countries(code)`, `unique(score_id, scope_type, scope_id, period_date, prompt_version)` |
| Índices | `(country_code, score_id, scope_id, period_date DESC)` · `(expires_at) where expires_at > now()` · GIN `citations` |
| RLS | SELECT público (cache hit); INSERT service_role únicamente (runtime cost guard). DELETE cron expired. |
| Fase | FASE 11 XL (bloque 11.E Causal Engine) |

### 17.11 `climate_twin_projections`

| Campo | Valor |
|---|---|
| Propósito | Proyecciones climáticas a 10/20/30 años por colonia (NOAA + CONAGUA + Atlas Riesgos). Temperatura, disponibilidad agua, calidad aire. Diferenciador "buy a house that will be there in 30 years". |
| Schema | `id bigserial pk`, `colonia_id uuid not null`, `projection_year int not null check (projection_year between 2030 and 2100)`, `baseline_year int not null default 2025`, `temp_celsius numeric` (avg anual proyectado), `temp_delta_vs_baseline numeric`, `water_availability_pct numeric check (water_availability_pct between 0 and 200)` (100=baseline), `air_quality_index numeric`, `flood_risk_score numeric`, `drought_risk_score numeric`, `confidence text check in ('high','medium','low','scenario_based')`, `scenario text check in ('RCP2.6','RCP4.5','RCP6.0','RCP8.5')`, `methodology_version text not null default 'v1'`, `components jsonb`, `country_code char(2) not null references countries(code)`, `unique(colonia_id, projection_year, scenario, methodology_version)` |
| Índices | `(country_code, colonia_id, projection_year, scenario)` · GIN `components` |
| RLS | SELECT público; INSERT service_role. |
| Fase | FASE 11 XL (bloque 11.N Climate Twin) |

### 17.12 `lifepath_user_profiles`

| Campo | Valor |
|---|---|
| Propósito | Perfil LifePath per usuario — inputs económicos/familiares/laborales + output top-3 zonas recomendadas con explicación. Motor matching basado en DMX-FAM/YNG/INV/STA. |
| Schema | `user_id uuid pk references profiles(id) on delete cascade`, `income_range text check in ('<15k','15-30k','30-60k','60-120k','120-250k','>250k')`, `family_state text check in ('single','couple','young_family','established_family','empty_nest','retiree')`, `work_mode text check in ('remote','hybrid','office','nomad')`, `preferences jsonb not null default '{}'::jsonb` (priorities: schools, commute, nightlife, green, budget), `top_3_matches jsonb`, `match_computed_at timestamptz`, `opt_in_alerts bool default false`, `country_code char(2) not null references countries(code)`, `created_at timestamptz default now()`, `updated_at timestamptz default now()` |
| Índices | `(country_code, family_state)` · GIN `preferences` · GIN `top_3_matches` |
| RLS | SELECT/UPDATE self + superadmin. INSERT self. Aggregated views (`lifepath_distribution`) público sin PII. |
| Fase | FASE 11 XL (bloque 11.O LifePath) |

### 17.13 `widget_embed_registry`

| Campo | Valor |
|---|---|
| Propósito | Registro widgets embebibles (`<iframe>` o JS SDK) para medios/blogs. Cada widget apunta a un scope + índice con customization (theme, size). Contador de views para attribution. |
| Schema | `embed_id uuid pk default gen_random_uuid()`, `owner_user_id uuid references profiles(id)`, `scope_type text check in ('colonia','alcaldia','ciudad','estado')`, `scope_id text not null`, `index_code text`, `customization jsonb not null default '{}'::jsonb` (theme, size, language), `active bool default true`, `views_count bigint default 0`, `last_view_at timestamptz`, `allowed_domains text[] default '{}'`, `country_code char(2) not null references countries(code)`, `created_at timestamptz default now()` |
| Índices | `(owner_user_id, active)` · `(scope_type, scope_id) where active` · GIN `allowed_domains` |
| RLS | SELECT owner + admin; público read via edge function con domain validation. INSERT authenticated + feature:widget_creator. |
| Fase | FASE 11 XL (bloque 11.R Widget embebible) |

### 17.14 `sticker_templates`

| Campo | Valor |
|---|---|
| Propósito | Templates generados (SVG/PNG) para stickers compartibles (Instagram/WhatsApp): "DMX Score 87 — Condesa Q3 2026" con branding. Downloads tracking. |
| Schema | `template_id uuid pk default gen_random_uuid()`, `template_type text check in ('score_card','index_badge','alert','wrapped','constellation')`, `svg_template text not null`, `customizable_fields jsonb not null` (scope_id, value, period, color_scheme), `downloads_count bigint default 0`, `active bool default true`, `version int default 1`, `country_code char(2)` (nullable — algunos templates cross-country), `created_at timestamptz default now()` |
| Índices | `(template_type, active)` · GIN `customizable_fields` |
| RLS | SELECT público todas; INSERT/UPDATE superadmin + designer. |
| Fase | FASE 11 XL (bloque 11.S Stickers) |

### 17.15 `ghost_zones_ranking`

| Campo | Valor |
|---|---|
| Propósito | Ranking zonas "fantasma" — zonas con score alto pero baja visibilidad (search + press). Detecta oportunidades antes del hype. Ghost Score = f(total_score, inverse_search_volume, inverse_press_mentions). |
| Schema | `id bigserial pk`, `colonia_id uuid not null`, `period_date date not null`, `score_total numeric not null`, `search_volume numeric`, `press_mentions int default 0`, `ghost_score numeric not null check (ghost_score between 0 and 100)`, `rank int`, `rank_total int`, `country_code char(2) not null references countries(code)`, `created_at timestamptz default now()`, `unique(colonia_id, period_date)` |
| Índices | `(country_code, period_date DESC, ghost_score DESC)` · `(period_date, rank) where rank is not null` |
| RLS | SELECT público período cerrado tier 1; último período tier 2+. INSERT service_role. |
| Fase | FASE 11 XL (bloque 11.M Ghost Zones) |

### 17.16 `scorecard_national_reports`

| Campo | Valor |
|---|---|
| Propósito | Reports "Scorecard Nacional" periódicos — snapshot agregado de índices DMX per país con PDF + narrativa + data snapshot. Insumo newsletter + prensa + B2B. |
| Schema | `report_id uuid pk default gen_random_uuid()`, `period_type text not null check in ('monthly','quarterly','yearly','wrapped')`, `period_date date not null`, `title text not null`, `pdf_url text`, `narrative_md text`, `data_snapshot jsonb not null` (indices_top10, movers, laggards, climate_alerts), `published_at timestamptz`, `published_by uuid references profiles(id)`, `draft bool default true`, `downloads_count bigint default 0`, `country_code char(2) not null references countries(code)`, `created_at timestamptz default now()`, `unique(period_type, period_date, country_code)` |
| Índices | `(country_code, period_type, period_date DESC)` · `(published_at DESC) where draft = false` · GIN `data_snapshot` |
| RLS | SELECT público `draft=false`; drafts superadmin + editor. INSERT editor (feature:scorecard_editor) + superadmin. |
| Fase | FASE 11 XL (bloque 11.U Scorecard Nacional) |

**Observación Dominio 17:** 16 tablas nuevas FASE 11 XL (1 ALTER + 15 CREATE). Todas RLS ON. Usan pgvector (17.6), pg_partman (17.1 + 17.3), GIN jsonb extensivo. RLS patrón dominante: SELECT público período cerrado, writes `service_role`. Dos excepciones owner-based: `zone_alert_subscriptions`, `lifepath_user_profiles`.

---

## Notas / disclaimer

1. Las tablas con **no-DELETE histórico** (audit_log, admin_actions, actividad_timeline, operation_timeline, interaction_feedback, unit_change_log, avance_obra_log, score_history, webhook_logs, xp_events) implementan RLS que niega DELETE a todos los roles excepto superadmin (para cumplir compliance y forensics).
2. Las tablas con PII (profiles, contactos, desarrolladoras, invoices) exponen VIEWs públicas (`public_profiles`, `public_desarrolladoras`, `public_projects`) sin columnas sensibles, consumidas por las landings y microsites.
3. `country_code` es `char(2)` no nullable en todas las tablas de dominio, con FK a `countries(code)`. Los índices compuestos siempre llevan `country_code` como primer elemento para particionamiento lógico per-país (futuro pg_partman H2).
4. Todas las operaciones de escritura sensible están cubiertas por triggers de audit o encoladas en cascadas IE (ver catálogo 03.3).
5. Los conteos exactos por dominio pueden variar +/- 2 tablas en la primera migration real tras Fase 01, dependiendo de cómo se materialicen vistas vs tablas en decisiones de performance.
6. **Dominio 15 (STR) + Dominio 16 (Infra) añadidos 2026-04-19** como parte de la reconciliación post-auditoría end-to-end, para reflejar el schema REAL actualmente en producción (62 tablas reales vs 96 planeadas originalmente). Las 16 str_* y 15 infra/ops NO se enumeraron con detalle campo-por-campo aquí (se deja como referencia a migration files); el detalle completo está en `supabase/migrations/20260419080000_*` → `20260419093000_*` para STR y `20260418035830_*` → `20260418091000_*` para infra.

---

**Autor original:** Claude Opus 4.7 (rewrite BATCH 2 Agent G) | **Fecha:** 2026-04-17
**Actualizaciones:** 2026-04-19 housekeeping post-07b (Dominio 14 PPD/Vibe Tags) + 2026-04-19 auditoría end-to-end (banner estado real, Dominios 15 STR y 16 Infra añadidos para reconciliar con remote).

---

## Append BLOQUES 11.M + 11.N (2026-04-23) — tablas shipped + ALTER

### 18.1 `vibe_tags` (BLOQUE 11.M)

| Campo | Valor |
|---|---|
| Propósito | Catálogo canónico de 10 vibe tags (walkability, quiet, nightlife, family, foodie, green, bohemian, corporate, safety_perceived, gentrifying) con labels es/en/pt. Alimenta Genoma similarity + chips UI. |
| Schema | `id text pk`, `label_es text not null`, `label_en text not null`, `label_pt text not null`, `sort_order integer not null default 0`, `created_at timestamptz default now()` |
| Seed | 10 tags canónicos insertados en migration `20260423100000_fase11_xl_vibe_tags.sql`. |
| RLS | `vibe_tags_public_read` (SELECT qual=true, comment `intentional_public`). `vibe_tags_service_write` (ALL service_role). |
| Fase | FASE 11 XL BLOQUE 11.M |
| Migration | `20260423100000_fase11_xl_vibe_tags.sql` |

### 18.2 `colonia_vibe_tags` (BLOQUE 11.M)

| Campo | Valor |
|---|---|
| Propósito | Pesos 0-100 por colonia × vibe_tag. H1 `source='heuristic_v1'` (determinístico sobre N0-N3 + índices DMX); reemplazable FASE 12 N5 a `source='llm_v1'` sin migración schema (ADR-022). |
| Schema | `colonia_id uuid`, `vibe_tag_id text references vibe_tags(id) on delete cascade`, `weight numeric(5,2) check 0..100`, `source text not null default 'heuristic_v1'`, `computed_at timestamptz default now()`, `primary key(colonia_id, vibe_tag_id)` |
| Índices | `(colonia_id, weight DESC)` · `(vibe_tag_id, weight DESC)` |
| RLS | `colonia_vibe_tags_public_read` (SELECT qual=true `intentional_public`). `service_write` ALL service_role. |
| Fase | FASE 11 XL BLOQUE 11.M |
| Migration | `20260423100000_fase11_xl_vibe_tags.sql` |

### 18.3 ALTER `futures_curve_projections` (BLOQUE 11.N)

| Campo | Valor |
|---|---|
| Propósito | Añade banda CI 95% explícita al forecast forward curve (vs escalar derivado). 8 columnas nuevas: `forward_3m_lower/upper`, `forward_6m_lower/upper`, `forward_12m_lower/upper`, `forward_24m_lower/upper`. Todas `numeric(6,2) NULL` → retrocompatible con rows existentes. |
| Schema ALTER | `alter table public.futures_curve_projections add column if not exists forward_{3,6,12,24}m_{lower,upper} numeric(6,2);` |
| RLS | Sin policies nuevas — columnas existen bajo policy `futures_curve_public_read` desde XL 11.A. |
| Fase | FASE 11 XL BLOQUE 11.N |
| Migration | `20260423110000_fase11_xl_futures_bands.sql` |

### 18.4 `pulse_forecasts` (BLOQUE 11.N, L93)

| Campo | Valor |
|---|---|
| Propósito | Forecast daily del Pulse Score 30 puntos por zona con banda CI 95%. Reemplazable LSTM/ARIMA FASE 12 N5 cambiando `methodology` label. |
| Schema | `id uuid pk default gen_random_uuid()`, `zone_id uuid not null`, `country_code text not null default 'MX'`, `forecast_date date not null`, `value numeric(5,2) check 0..100`, `value_lower numeric(5,2) check 0..100`, `value_upper numeric(5,2) check 0..100`, `methodology text default 'heuristic_v1'`, `generated_at timestamptz default now()`, `unique(zone_id, forecast_date, methodology)` |
| Índices | `(zone_id, forecast_date DESC)` · `(country_code, forecast_date DESC)` |
| RLS | `pulse_forecasts_public_read` (SELECT qual=true `intentional_public`). `service_write` ALL service_role. |
| Fase | FASE 11 XL BLOQUE 11.N |
| Migration | `20260423110100_fase11_xl_pulse_forecasts.sql` |

### 18.5 Decisión técnica: REUSO `colonia_dna_vectors` (no se creó `colonia_embeddings`)

El prompt original 11.M pedía crear `colonia_embeddings` nueva, pero `colonia_dna_vectors` ya existe desde FASE 11 XL BLOQUE 11.A (`20260421100000_fase11_xl_dmx_indices_schema.sql`) con estructura idéntica: `vector(64)` + HNSW cosine + RLS public_read. Crear una segunda tabla con mismo propósito = dual source of truth (deuda técnica). Se reusa la existente; campo `methodology_version` actúa como `features_version` semántico. Diferencia vs prompt documentada en commit SHA `0035640`.

**Observación BLOQUES 11.M+11.N:** 3 tablas nuevas (vibe_tags + colonia_vibe_tags + pulse_forecasts) + 1 ALTER retrocompatible (futures_curve_projections). Todas RLS ON con `intentional_public` comment pattern. Allowlist bumps v20 (11.M) + v21 (11.N).

**Autor append 11.M+11.N:** Manu Acosta + Claude Opus 4.7 | **Fecha:** 2026-04-23 | **Status:** Shipped remote (supabase db push --linked exitoso).

## Append BLOQUES 11.O + 11.P (2026-04-23 tarde) — tablas shipped + ALTER + rename + refactor pgvector

### 19.1 ALTER `lifepath_user_profiles` (BLOQUE 11.O — reuso in-place)

| Campo | Valor |
|---|---|
| Propósito | Reuso 100% de tabla existente (XL 11.A §17.12) con ALTER mínimo. Founder decisión Opción B refinada (2026-04-23): evitar sobre-ingeniería en SEED H1 — jsonb inline top-20 matches vs tabla separada normalizada. Splitteable en FASE 12 N5 (L138 LifePath AI). |
| Schema ALTER | `rename column top_3_matches to matches;` + `add column answers_version text not null default 'v1';` + `add column methodology text not null default 'heuristic_v1';` |
| RLS | Sin cambios — policies owner-based `user_id = auth.uid()` + `service_role all` ya correctas. |
| Fase | FASE 11 XL BLOQUE 11.O |
| Migration | `20260423120000_fase11_xl_lifepath_alter.sql` |

### 19.2 RENAME `climate_twin_projections` → `climate_future_projections` (BLOQUE 11.P — separación semántica)

| Campo | Valor |
|---|---|
| Propósito | Separar Clima Futuro (H2 proyecciones 2040/2050 — tabla existente XL 11.A §17.11) de Clima Gemelo Histórico (H1 nuevo — 11.P). 0 referencias TS/TSX en código (solo database.ts auto-generado), rename seguro. |
| Schema ALTER | `alter table climate_twin_projections rename to climate_future_projections;` + `alter policy climate_twin_public_read rename to climate_future_public_read;` + `alter policy climate_twin_service_write rename to climate_future_service_write;` |
| RLS | Policies renombradas; comment `intentional_public` persiste (ALTER POLICY RENAME no toca comments). |
| Fase | FASE 11 XL BLOQUE 11.P |
| Migration | `20260423120100_fase11_xl_climate_twin_schema.sql` |

### 19.3 `climate_monthly_aggregates` (BLOQUE 11.P — nueva)

| Campo | Valor |
|---|---|
| Propósito | Aggregates mensuales NOAA+CONAGUA 15 años × 200 colonias CDMX SEED. Feed del Clima Gemelo histórico. Escala: 180 meses × 200 zonas = 36K rows (cabe en Supabase 8GB free). |
| Schema | `zone_id uuid not null`, `year_month date not null`, `temp_avg numeric(5,2)`, `temp_max numeric(5,2)`, `temp_min numeric(5,2)`, `rainfall_mm numeric(7,2)`, `humidity_avg numeric(5,2) check 0..100`, `extreme_events_count jsonb default '{}'`, `source text check in ('heuristic_v1','noaa','conagua','hybrid') default 'heuristic_v1'`, `computed_at timestamptz default now()`, `unique (zone_id, year_month)` |
| Índices | `(zone_id, year_month DESC)` · `(year_month)` |
| RLS | `climate_monthly_public_read` (SELECT qual=true `intentional_public`). `service_write` ALL service_role. |
| Fase | FASE 11 XL BLOQUE 11.P |
| Migration | `20260423120100_fase11_xl_climate_twin_schema.sql` + check constraint update `20260423130100_fase11_xl_climate_source_heuristic.sql` |

### 19.4 `climate_annual_summaries` (BLOQUE 11.P — refactor pgvector + HNSW)

| Campo | Valor |
|---|---|
| Propósito | Summary anual + signature vector(12) normalizado [0..1] canónico (12 features: temp_avg · temp_range · rainfall_total_y · rainfall_variability · humidity_avg · humidity_range · extreme_heat_days · extreme_cold_days · flood_risk_score · drought_risk_score · seasonality_index · climate_change_delta). Refactor post-SEED: `numeric[]` → `vector(12)` + HNSW cosine index (filosofía escalable, 0 rows pre-refactor). |
| Schema | `zone_id uuid not null`, `year integer check 2010..2030`, `climate_type text check in ('tropical','arid','temperate','cold','humid_subtropical')`, `composite_climate_signature vector(12) not null`, `summary jsonb default '{}'`, `computed_at timestamptz default now()`, `unique (zone_id, year)` |
| Índices | `(zone_id, year DESC)` · **HNSW** `(composite_climate_signature vector_cosine_ops)` — similarity search O(log N) DB-side |
| RLS | `climate_annual_public_read` (SELECT qual=true `intentional_public`). `service_write` ALL service_role. |
| Fase | FASE 11 XL BLOQUE 11.P (nueva → DROP+CREATE refactor pgvector en fix commit) |
| Migrations | `20260423120100_fase11_xl_climate_twin_schema.sql` + refactor `20260423130000_fase11_xl_climate_pgvector_refactor.sql` |

### 19.5 `climate_zone_signatures` (BLOQUE 11.P — nueva aggregate per-zone)

| Campo | Valor |
|---|---|
| Propósito | Signature agregado per-zone (mean over years + climate_change_delta) para twin matching eficiente DB-side. Input del RPC `find_climate_twins`. |
| Schema | `zone_id uuid primary key`, `signature vector(12) not null`, `years_observed integer default 0`, `methodology text default 'heuristic_v1'`, `computed_at timestamptz default now()` |
| Índices | **HNSW** `(signature vector_cosine_ops)` — O(log N) |
| RLS | `climate_zone_sig_public_read` (SELECT qual=true `intentional_public`). `service_write` ALL service_role. |
| Fase | FASE 11 XL BLOQUE 11.P (fix refactor) |
| Migration | `20260423130000_fase11_xl_climate_pgvector_refactor.sql` |

### 19.6 `climate_twin_matches` (BLOQUE 11.P — nueva cache twin similarity)

| Campo | Valor |
|---|---|
| Propósito | Cache persistida de resultados del RPC `find_climate_twins`. Zone × twin_zone con similarity 0-100 + shared_patterns jsonb (features con diff < 0.1). |
| Schema | `zone_id uuid not null`, `twin_zone_id uuid not null check <> zone_id`, `similarity numeric(5,2) check 0..100`, `shared_patterns jsonb default '{}'`, `methodology text default 'heuristic_v1'`, `computed_at timestamptz default now()`, `unique (zone_id, twin_zone_id)` |
| Índices | `(zone_id, similarity DESC)` |
| RLS | `climate_twin_matches_public_read` (SELECT qual=true `intentional_public`). `service_write` ALL service_role. |
| Fase | FASE 11 XL BLOQUE 11.P |
| Migration | `20260423120100_fase11_xl_climate_twin_schema.sql` |

### 19.7 RPC `find_climate_twins(p_zone_id uuid, p_top_n int, p_min_sim numeric)` (BLOQUE 11.P — nueva)

| Campo | Valor |
|---|---|
| Propósito | Twin matching O(log N) DB-side vía HNSW cosine sobre `climate_zone_signatures`. Retorna top N twins con similarity 0-100. Llamable desde supabase-js `.rpc('find_climate_twins', {...})`. |
| Signature | `returns table (twin_zone_id uuid, similarity numeric)` · language sql · stable · security definer · `set search_path = ''` · `OPERATOR(public.<=>)` qualified (pgvector extension en schema `public`). |
| Allowlist | Añadido a `audit_rls_allowlist_v23` exclude list (SECDEF stable read-only). |
| Fase | FASE 11 XL BLOQUE 11.P (fix refactor) |
| Migration | `20260423130000_fase11_xl_climate_pgvector_refactor.sql` |

### 19.8 Observaciones BLOQUES 11.O + 11.P

- **Decisión Opción B refinada (founder 2026-04-23):** ALTER in-place `lifepath_user_profiles` (no splittear matches a tabla normalizada) + rename climate futuro (separar Clima Futuro H2 de Clima Gemelo H1) + 3 tablas climate nuevas.
- **Refactor escalable pgvector:** post-SEED se refactorizó `climate_annual_summaries` de `numeric[]` → `vector(12)` + HNSW cosine + nueva `climate_zone_signatures` aggregate per-zone + RPC `find_climate_twins` DB-side. Filosofía canonizada (feedback_arquitectura_escalable_desacoplada.md) — sub-lineal a escala nacional 10K+ zonas.
- **Correcciones iterativas durante push remote:** (1) `search_path = 'public, extensions'` string quoted se interpretaba como identifier único → fix a `set search_path = ''` + `OPERATOR(public.<=>)` qualified; (2) pgvector extension vive en schema `public` (no `extensions`) en este proyecto → `OPERATOR(public.<=>)`.
- **Allowlist bumps:** v22 (11.O+P initial) + v23 (11.O+P fix refactor).
- **Total migrations 11.O+P:** 6 aplicadas remote vía `supabase db push --linked` manual (lesson 11.J activa).

**Autor append 11.O+11.P:** Manu Acosta + Claude Opus 4.7 | **Fecha:** 2026-04-23 tarde | **Status:** Shipped remote (v23 tip, main SHA 9f6442f, PR #28).

## Append BLOQUES 11.Q + 11.R (2026-04-24) — clusters table + indexes escalables + allowlist v24 + uso ghost_zones

### 20.1 `zone_constellation_clusters` (BLOQUE 11.R — nueva, Louvain output)

| Campo | Valor |
|---|---|
| Propósito | Persistir asignación `cluster_id` por zona × período resultado de Louvain community detection (Blondel fase 1 JS puro sobre `zone_constellations_edges`). Consumida por UI D3 graph coloring + queries "colonias del mismo cluster que X". Tabla separada normalizada (vs jsonb inline summary) — filosofía escalable: queryable por cluster_id DESC por size. |
| Schema | `id uuid primary key default gen_random_uuid()`, `zone_id uuid not null`, `cluster_id int not null`, `period_date date not null`, `computed_at timestamptz default now()`, `unique (zone_id, period_date)` |
| Índices | `(period_date, cluster_id)` — listado clusters por período · `(zone_id, period_date DESC)` — cluster actual de zona |
| RLS | `zone_constellation_clusters_public_read` (SELECT qual=true `intentional_public` — features agregadas sin PII, consistente con pulse/genome) · `service_write` ALL service_role. |
| Fase | FASE 11 XL BLOQUE 11.R |
| Migration | `20260424100100_fase11_xl_constellation_clusters.sql` |

### 20.2 Index GIN `zone_constellations_edges_types_gin` (BLOQUE 11.R — nueva)

| Campo | Valor |
|---|---|
| Propósito | Filtrado O(log N) por `edge_types jsonb` en hot path UI sliders (U5) + contagion paths (U13) + Futures correlation boost (U15). A escala nacional 10k+ edges, sin este índice queries serían seq scan. Con GIN `jsonb_path_ops`: sub-lineal igual que pgvector HNSW en Climate Twin (11.P). |
| Schema | `create index zone_constellations_edges_types_gin on public.zone_constellations_edges using gin (edge_types jsonb_path_ops)` |
| Uso | Predicados `edge_types->>'migration' > 50`, `edge_types @> '{"genoma_similarity": 70}'`, etc. |
| Fase | FASE 11 XL BLOQUE 11.R |
| Migration | `20260424100000_fase11_xl_constellations_gin_index.sql` |

### 20.3 Index BTree `zone_constellations_edges_weight_idx` (BLOQUE 11.R — nueva)

| Campo | Valor |
|---|---|
| Propósito | Acelera "top-N neighbors per zone" query que alimenta el graph viewport (UI focalizada por `coloniaId`). Sin este índice: full table scan sobre `edge_weight` al cargar foco. |
| Schema | `create index zone_constellations_edges_weight_idx on public.zone_constellations_edges (source_colonia_id, edge_weight DESC)` |
| Uso | `.from('zone_constellations_edges').eq('source_colonia_id', x).order('edge_weight', {ascending: false}).limit(50)` — hot path Graph focalizado. |
| Fase | FASE 11 XL BLOQUE 11.R |
| Migration | `20260424100000_fase11_xl_constellations_gin_index.sql` |

### 20.4 `audit_rls_allowlist_v24` (BLOQUE 11.R — bump)

| Campo | Valor |
|---|---|
| Propósito | Clone de v23 (11.O+P tip) con comment actualizado cubriendo nueva `zone_constellation_clusters` table (policies `intentional_public` read + `service_role` write) + indexes GIN/BTree nuevos. Sin SECDEF functions nuevas — engine Louvain + path finder corren en Node. |
| Schema | `create or replace function public.audit_rls_violations() returns table(...) language plpgsql security definer set search_path = ''` + `comment on function ... is 'v24 — FASE 11 XL BLOQUE 11.R ...'` |
| Exclude list SECDEF | Mantiene `find_climate_twins` (11.P) + lista pre-existente. Sin nuevas entries. |
| Fase | FASE 11 XL BLOQUE 11.R |
| Migration | `20260424100200_audit_rls_allowlist_v24.sql` |

### 20.5 `ghost_zones_ranking` (BLOQUE 11.Q — uso + nota RLS)

| Campo | Valor |
|---|---|
| Propósito | Tabla schema-native pre-existente (XL 11.A) consumida por primera vez en 11.Q. Persiste ranking mensual de colonias con `ghost_score`, `rank`, `search_volume`, `press_mentions`, `score_total` (DMX avg inline). UNIQUE `(colonia_id, period_date)` idempotente. |
| Schema | existente XL 11.A — `colonia_id`, `country_code`, `period_date`, `ghost_score numeric(5,2)`, `rank int`, `search_volume int default 0`, `press_mentions int default 0`, `score_total numeric(5,2)`, `calculated_at timestamptz default now()` |
| RLS política | **Authenticated-only** (Pro+ gated vía `authenticatedProcedure` tRPC) — NO `intentional_public` comment. Ruta UI `/indices/ghost-zones/ranking` usa robots noindex + fallback CTA signup cuando UNAUTHORIZED. |
| Uso | `trpc.ghostZones.ranking` (top N por período) · `trpc.ghostZones.timeline12m` (histórico por colonia) · `trpc.ghostZones.topOverHypedIds` (helper cross-function U14 LifePath) · cron fan-out master monthly. |
| Fase | FASE 11 XL BLOQUE 11.Q (primera activación) |
| Migration | ninguna nueva en 11.Q (usa schema existente XL 11.A) |

### 20.6 Observaciones BLOQUES 11.Q + 11.R

- **Filosofía escalable aplicada (memoria `feedback_arquitectura_escalable_desacoplada.md`):** GIN `jsonb_path_ops` + BTree compuesto permiten queries O(log N) a escala nacional 10k+ edges sin refactor — escalamiento asincrónico al pasar de CDMX a MX completo (10K+ colonias).
- **Decisión tabla nueva vs jsonb inline:** founder cerró D4 (2026-04-24) a favor de tabla `zone_constellation_clusters` separada normalizada — queryable (top clusters por size, "colonias del mismo cluster"), vs jsonb summary inline que habría forzado full-scan para esas queries.
- **Allowlist bump +1:** v23 → v24 (1 sola bump BLOQUE 11.R, porque 11.Q usó tabla existente sin policies nuevas).
- **Sin SECDEF nuevas:** engine Constellations (Louvain + path finder + edge builder) corre 100% en Node (shared/lib/intelligence-engine/constellations). Contrasta con 11.P Climate Twin que requirió RPC `find_climate_twins` por HNSW DB-side.
- **Ghost Zones auth-only:** decisión D2 (founder 2026-04-24) — schema-native gating via `authenticatedProcedure` sin extra RLS policy column. Landing público sin data = marketing teaser; ranking Pro+ Paid.
- **Total migrations 11.Q+R:** 3 aplicadas remote vía `supabase db push --linked` manual (lesson 11.J activa, ritual consolidado).

**Autor append 11.Q+11.R:** Manu Acosta + Claude Opus 4.7 | **Fecha:** 2026-04-24 | **Status:** Shipped remote (v24 tip, main SHA acb7d16, PR #30).
